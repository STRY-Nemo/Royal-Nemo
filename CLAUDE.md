# Project notes for coding agents

- The user works from a phone. End every task with the link to open:
  **https://ryanrhernandez-design.github.io/Royal-Nemo/** (and the PR link when relevant).
- Spec lives in `docs/spec/`; status and handoff notes in `PROGRESS.md`.
- Run `npm run typecheck && npm test && npm run test:server` before pushing.

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });

// ../src/data/members.json
var members_default = [
  {
    member_id: "stry-001",
    username: "Mario AK47",
    alliance_rank: "R4",
    origin_alliance: "ROYL",
    level: 35,
    arena_power_m: 836.8,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-002",
    username: "IcemanPowaaa",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 34,
    arena_power_m: 424.4,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-003",
    username: "Appins",
    alliance_rank: "R4",
    origin_alliance: "ROYL",
    level: 35,
    arena_power_m: 301.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-004",
    username: "Rrrrrrrrrd",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 34,
    arena_power_m: 299.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-005",
    username: "Ultra EGO",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 35,
    arena_power_m: 295.1,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-006",
    username: "Nemo Hoes",
    alliance_rank: "R4",
    origin_alliance: "ROYL",
    level: 35,
    arena_power_m: 278,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-007",
    username: "DeeDeeeee",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 35,
    arena_power_m: 265.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-008",
    username: "Queen Rouge",
    alliance_rank: "R5",
    origin_alliance: "STRY",
    level: 33,
    arena_power_m: 264.1,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-009",
    username: "Dorin",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 35,
    arena_power_m: 261.6,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-010",
    username: "Shastorm",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 31,
    arena_power_m: 240.8,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-011",
    username: "Apparition-",
    alliance_rank: "R4",
    origin_alliance: "ROYL",
    level: 35,
    arena_power_m: 239.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-012",
    username: "R8drz4life",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 33,
    arena_power_m: 222.1,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-013",
    username: "RockyNoSpeedUps",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 34,
    arena_power_m: 215.2,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-014",
    username: "Dvon Khan",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 35,
    arena_power_m: 213.1,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-015",
    username: "-PIM-",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 35,
    arena_power_m: 211,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-016",
    username: "Sats0mNlak",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 32,
    arena_power_m: 207.2,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-017",
    username: "JoelitoBB",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 34,
    arena_power_m: 206.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-018",
    username: "SwiftBunny DH",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 33,
    arena_power_m: 206,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-019",
    username: "JAY QUEEN",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 35,
    arena_power_m: 204.4,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-020",
    username: "T\u1EBDmuj\xEFn",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 34,
    arena_power_m: 203,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-021",
    username: "Captain Cake",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 34,
    arena_power_m: 202.6,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-022",
    username: "Victoria13",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 34,
    arena_power_m: 201.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-023",
    username: "CooksLS9",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 34,
    arena_power_m: 201.1,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-024",
    username: "Mada",
    alliance_rank: "R4",
    origin_alliance: "CAPO",
    level: 32,
    arena_power_m: 199.6,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-025",
    username: "\u2022 Dyo \u2022",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 32,
    arena_power_m: 199.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-026",
    username: "UndertakerGreg",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 32,
    arena_power_m: 198.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-027",
    username: "Elmo",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 35,
    arena_power_m: 197.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-028",
    username: "Chika Strike",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 32,
    arena_power_m: 197.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-029",
    username: "SnoopyB",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 35,
    arena_power_m: 196.2,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-030",
    username: "hausshavoc",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 31,
    arena_power_m: 193.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-031",
    username: "MaKaRa",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 34,
    arena_power_m: 193.6,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-032",
    username: "Giovanni Savage",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 35,
    arena_power_m: 192.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-033",
    username: "KEV1980",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 33,
    arena_power_m: 191.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-034",
    username: "raZ",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 34,
    arena_power_m: 190.8,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-035",
    username: "Dinkleberg6969",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 32,
    arena_power_m: 188.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-036",
    username: "MathSic",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 35,
    arena_power_m: 187.2,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-037",
    username: "Tubbie 300BLK",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 33,
    arena_power_m: 186.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-038",
    username: "Kane \u2022",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 34,
    arena_power_m: 186.5,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-039",
    username: "PablitoSnow",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 183.8,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-040",
    username: "JetPed",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 31,
    arena_power_m: 183,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-041",
    username: "Flowmotion47",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 30,
    arena_power_m: 182.8,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-042",
    username: "Mapinski",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 35,
    arena_power_m: 181.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-043",
    username: "RosolinoFriddi",
    alliance_rank: "R4",
    origin_alliance: "CAPO",
    level: 32,
    arena_power_m: 180.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-044",
    username: "\u2022 TooN \u2022",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 34,
    arena_power_m: 179.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-045",
    username: "Matt02",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 33,
    arena_power_m: 179.4,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-046",
    username: "PajuHalfLapin",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 31,
    arena_power_m: 178.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-047",
    username: "DemonKingg",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 33,
    arena_power_m: 178.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-048",
    username: "Provh",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 178,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-049",
    username: "Twist of Fate",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 32,
    arena_power_m: 177.2,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-050",
    username: "Anton S",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 34,
    arena_power_m: 176.6,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-051",
    username: "-Yeti-",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 32,
    arena_power_m: 174.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-052",
    username: "SophieUa",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 32,
    arena_power_m: 174.4,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-053",
    username: "LoC187",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 34,
    arena_power_m: 173.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-054",
    username: "levON",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 32,
    arena_power_m: 173.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-055",
    username: "TinaG",
    alliance_rank: "R4",
    origin_alliance: "STRY",
    level: 33,
    arena_power_m: 173.2,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-056",
    username: "Wakeboy",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 31,
    arena_power_m: 170.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-057",
    username: "Thir13en",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 33,
    arena_power_m: 167.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-058",
    username: "KingLow",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 33,
    arena_power_m: 165,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-059",
    username: "InkedWitch",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 34,
    arena_power_m: 164.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-060",
    username: "MamaBt",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 32,
    arena_power_m: 164.5,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-061",
    username: "Galihad",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 32,
    arena_power_m: 162.5,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-062",
    username: "USO Kiwi",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 32,
    arena_power_m: 161.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-063",
    username: "Pappless",
    alliance_rank: "R4",
    origin_alliance: "CAPO",
    level: 31,
    arena_power_m: 158.8,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-064",
    username: "DontMesswNess",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 33,
    arena_power_m: 158.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-065",
    username: "Bachter",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 32,
    arena_power_m: 158.2,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-066",
    username: "Dr House 96",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 31,
    arena_power_m: 158.1,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-067",
    username: "Sanatio Fax",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 31,
    arena_power_m: 157.4,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-068",
    username: "Pmari Snow",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 33,
    arena_power_m: 157.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-069",
    username: "pi nk",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 31,
    arena_power_m: 156.8,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-070",
    username: "King of goats",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 31,
    arena_power_m: 156.8,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-071",
    username: "MysticPotato",
    alliance_rank: "R3",
    origin_alliance: "ROYL",
    level: 33,
    arena_power_m: 155.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-072",
    username: "Pandoraloft",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 31,
    arena_power_m: 153.1,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-073",
    username: "AvatarNavi",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 33,
    arena_power_m: 152.6,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-074",
    username: "Potepuhec",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 33,
    arena_power_m: 151.5,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-075",
    username: "Raul863",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 32,
    arena_power_m: 147.1,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-076",
    username: "Kotsos rs6",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 144.5,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-077",
    username: "Warriorwarrior",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 31,
    arena_power_m: 142.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-078",
    username: "Krakenasaurus",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 32,
    arena_power_m: 142.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-079",
    username: "kikimyu",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 142,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-080",
    username: "Thrykika",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 140.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-081",
    username: "fivos tech",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 139.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-082",
    username: "the david",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 30,
    arena_power_m: 139,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-083",
    username: "-Aleks-",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 31,
    arena_power_m: 138.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-084",
    username: "Jeanniee",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 31,
    arena_power_m: 136.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-085",
    username: "crumbum271",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 135,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-086",
    username: "LostSoul1213",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 29,
    arena_power_m: 134.5,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-087",
    username: "Wanderer629",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 133.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-088",
    username: "Bigfor86",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 32,
    arena_power_m: 131.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-089",
    username: "SantiSalice",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 31,
    arena_power_m: 130.7,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-090",
    username: "Robamboo",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 30,
    arena_power_m: 130.1,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-091",
    username: "pepito",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 28,
    arena_power_m: 127.8,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-092",
    username: "Luigi Banana",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 125.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-093",
    username: "Texas Roman",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 30,
    arena_power_m: 124.4,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-094",
    username: "Pitour",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 32,
    arena_power_m: 124.3,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-095",
    username: "Momit",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 29,
    arena_power_m: 121.1,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-096",
    username: "Royal09",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 115.9,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-097",
    username: "coincoin18",
    alliance_rank: "R3",
    origin_alliance: "STRY",
    level: 30,
    arena_power_m: 115.8,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-098",
    username: "Oggie83",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 30,
    arena_power_m: 73.2,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-099",
    username: "Swarly",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 30,
    arena_power_m: 71.6,
    active: true,
    power_as_of: "2026-09-07"
  },
  {
    member_id: "stry-100",
    username: "Black-wolff",
    alliance_rank: "R3",
    origin_alliance: "CAPO",
    level: 28,
    arena_power_m: 62.4,
    active: true,
    power_as_of: "2026-09-07"
  }
];

// ../src/data/responsibilities.json
var responsibilities_default = {
  source: "STRY_Leadership_Responsibilities_Updated_v2.xlsx",
  identity_mapping_status: "needs_review",
  dropdown_names: [
    "Rouge",
    "Nemo",
    "Appins",
    "Apparition",
    "Roso",
    "Mada",
    "TBD/Rotation",
    "TBD/Capo",
    "Paju",
    "Mario",
    "TBD"
  ],
  added_option: "TBD added from prior user requirement; absent in latest source validation",
  responsibilities: [
    {
      id: "responsibility-01",
      title: "Glory Wars (GW)",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: "Appins",
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: "Rouge",
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: "Paju",
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-02",
      title: "SVS",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: "Nemo",
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: "Rouge",
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-03",
      title: "Volcano Wars",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: null,
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-04",
      title: "Canyon",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: "Apparition",
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: "Roso",
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-05",
      title: "Research Priorities",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: null,
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-06",
      title: "Alliance Shop",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: null,
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-07",
      title: "Reminders",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: null,
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-08",
      title: "Polls / Voting",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: "Mada",
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-09",
      title: "Trade Hubs",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: "Rouge",
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: "Nemo",
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-10",
      title: "Kongs",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: "Nemo",
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-11",
      title: "VS Strategy",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: "Appins",
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-12",
      title: "Alliance Mail / Major Announcements",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: "Rouge",
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: "Nemo",
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: "Roso",
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: "Appins",
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-13",
      title: "Event Calendar / Scheduling",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: null,
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-14",
      title: "Member Activity / Participation",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: null,
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-15",
      title: "New Member Onboarding",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: null,
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    },
    {
      id: "responsibility-16",
      title: "Diplomacy / Server Relations",
      slots: [
        {
          position: 1,
          label: "Lead",
          source_name: null,
          member_id: null
        },
        {
          position: 2,
          label: "Assigned 2",
          source_name: null,
          member_id: null
        },
        {
          position: 3,
          label: "Assigned 3",
          source_name: null,
          member_id: null
        },
        {
          position: 4,
          label: "Assigned 4",
          source_name: null,
          member_id: null
        }
      ]
    }
  ]
};

// ../src/data/seed.ts
var PACKAGE_DATE = "2026-09-09";
var SERIES_ID = "canyon-friday";
function loadSeedMembers() {
  const raw = members_default;
  return raw.map((m) => ({
    id: m.member_id,
    username: m.username,
    aliases: [],
    rank: m.alliance_rank,
    origin_alliance: m.origin_alliance,
    level: m.level,
    arena_power_m: m.arena_power_m,
    power_as_of: m.power_as_of,
    active: m.active,
    tracking_start: PACKAGE_DATE
  }));
}
__name(loadSeedMembers, "loadSeedMembers");
var PLACEHOLDER_NAMES = ["TBD", "TBD/Rotation", "TBD/Capo"];
function loadSeedOrganization() {
  const raw = responsibilities_default;
  const responsibilities = raw.responsibilities.map((r, index) => ({
    id: r.id,
    title: r.title,
    order: index,
    archived: false,
    slots: r.slots.map(
      (s) => ({
        position: s.position,
        label: s.label,
        source_name: s.source_name,
        member_id: s.member_id,
        placeholder: s.source_name && PLACEHOLDER_NAMES.includes(s.source_name) ? s.source_name : null
      })
    )
  }));
  const names = raw.dropdown_names.includes("TBD") ? raw.dropdown_names : [...raw.dropdown_names, "TBD"];
  return { responsibilities, dropdown_names: names, name_mapping: {}, revision: 1 };
}
__name(loadSeedOrganization, "loadSeedOrganization");

// ../src/engine/history.ts
var DEFAULT_WINDOW_SIZE = 8;
function finalStarters(event) {
  const last = event.published_revisions[event.published_revisions.length - 1];
  const source = last ? last.assignments : [];
  return source.filter((a) => a.role === "starter");
}
__name(finalStarters, "finalStarters");
function wasExplicitlyAvailable(event, memberId) {
  const a = event.availability[memberId];
  return !!a && a.choice !== "unavailable";
}
__name(wasExplicitlyAvailable, "wasExplicitlyAvailable");
function computeHistory(events, members, options = {}) {
  const windowSize = options.windowSize ?? DEFAULT_WINDOW_SIZE;
  const unique = /* @__PURE__ */ new Map();
  for (const e of events) unique.set(e.id, e);
  const finalized = [...unique.values()].filter((e) => e.status === "finalized").sort((a, b) => a.date < b.date ? -1 : a.date > b.date ? 1 : a.id < b.id ? -1 : 1);
  const result = {};
  for (const member of members) {
    const tracked = finalized.filter((e) => e.date >= member.tracking_start);
    const windowEvents = tracked.slice(Math.max(0, tracked.length - windowSize));
    let lifetimePlayed = 0;
    let lifetimeBenches = 0;
    let lastPlayed = null;
    const classify = /* @__PURE__ */ __name((event) => {
      const att = event.attendance[member.id];
      const outcome = att?.outcome ?? null;
      if (outcome === "played") return "played";
      if (outcome === "no_show") return "no_show";
      if (outcome === "withdrew") return "withdrew";
      const starter = finalStarters(event).some((a) => a.member_id === member.id);
      if (starter && (outcome === null || outcome === "unknown")) return "unknown";
      if (wasExplicitlyAvailable(event, member.id) && !starter) return "bench";
      if (outcome === "unknown") return "unknown";
      return "none";
    }, "classify");
    for (const event of tracked) {
      const c = classify(event);
      if (c === "played") {
        lifetimePlayed++;
        if (!lastPlayed || event.date > lastPlayed) lastPlayed = event.date;
      } else if (c === "bench") {
        lifetimeBenches++;
      }
    }
    let played = 0;
    let benches = 0;
    let unknown = 0;
    let noShow = 0;
    let withdrew = 0;
    for (const event of windowEvents) {
      const c = classify(event);
      if (c === "played") played++;
      else if (c === "bench") benches++;
      else if (c === "unknown") unknown++;
      else if (c === "no_show") noShow++;
      else if (c === "withdrew") withdrew++;
    }
    result[member.id] = {
      member_id: member.id,
      played_count: played,
      eligible_benches: benches,
      last_played_at: lastPlayed,
      lifetime_played: lifetimePlayed,
      lifetime_benches: lifetimeBenches,
      events_in_window: windowEvents.length,
      unknown_count: unknown,
      no_show_count: noShow,
      withdrew_count: withdrew,
      history_incomplete: windowEvents.length < windowSize,
      waiting_since: lastPlayed ?? member.tracking_start
    };
  }
  return result;
}
__name(computeHistory, "computeHistory");
function emptyHistory(member) {
  return {
    member_id: member.id,
    played_count: 0,
    eligible_benches: 0,
    last_played_at: null,
    lifetime_played: 0,
    lifetime_benches: 0,
    events_in_window: 0,
    unknown_count: 0,
    no_show_count: 0,
    withdrew_count: 0,
    history_incomplete: true,
    waiting_since: member.tracking_start
  };
}
__name(emptyHistory, "emptyHistory");

// ../src/engine/recurrence.ts
var FRIDAY = 5;
function parseDate(iso) {
  const m = /^(\d{4})-(\d{2})-(\d{2})$/.exec(iso);
  if (!m) throw new Error(`Invalid ISO date: ${iso}`);
  return { year: Number(m[1]), month: Number(m[2]), day: Number(m[3]) };
}
__name(parseDate, "parseDate");
function formatDate(d) {
  const mm = String(d.month).padStart(2, "0");
  const dd = String(d.day).padStart(2, "0");
  return `${d.year}-${mm}-${dd}`;
}
__name(formatDate, "formatDate");
function addDays(iso, days) {
  const d = parseDate(iso);
  const ms = Date.UTC(d.year, d.month - 1, d.day) + days * 864e5;
  const out = new Date(ms);
  return formatDate({ year: out.getUTCFullYear(), month: out.getUTCMonth() + 1, day: out.getUTCDate() });
}
__name(addDays, "addDays");
function weekday(iso) {
  const d = parseDate(iso);
  return new Date(Date.UTC(d.year, d.month - 1, d.day)).getUTCDay();
}
__name(weekday, "weekday");
function nextFriday(fromDate, inclusive = false) {
  const wd = weekday(fromDate);
  let delta = (FRIDAY - wd + 7) % 7;
  if (delta === 0 && !inclusive) delta = 7;
  return addDays(fromDate, delta);
}
__name(nextFriday, "nextFriday");
function eventIdFor(seriesId, date) {
  return `${seriesId}:${date}`;
}
__name(eventIdFor, "eventIdFor");
function isValidTimeZone(tz) {
  if (!tz) return false;
  try {
    new Intl.DateTimeFormat("en-US", { timeZone: tz }).format(/* @__PURE__ */ new Date());
    return true;
  } catch {
    return false;
  }
}
__name(isValidTimeZone, "isValidTimeZone");
var partsCache = /* @__PURE__ */ new Map();
function formatter(tz) {
  let f = partsCache.get(tz);
  if (!f) {
    f = new Intl.DateTimeFormat("en-US", {
      timeZone: tz,
      hourCycle: "h23",
      year: "numeric",
      month: "2-digit",
      day: "2-digit",
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit"
    });
    partsCache.set(tz, f);
  }
  return f;
}
__name(formatter, "formatter");
function zonedParts(utcMs, tz) {
  const parts = formatter(tz).formatToParts(new Date(utcMs));
  const get = /* @__PURE__ */ __name((type) => Number(parts.find((p) => p.type === type)?.value ?? "0"), "get");
  const hour = get("hour");
  return {
    year: get("year"),
    month: get("month"),
    day: get("day"),
    hour: hour === 24 ? 0 : hour,
    minute: get("minute"),
    second: get("second")
  };
}
__name(zonedParts, "zonedParts");
function todayInZone(tz, now = /* @__PURE__ */ new Date()) {
  const p = zonedParts(now.getTime(), tz);
  return formatDate({ year: p.year, month: p.month, day: p.day });
}
__name(todayInZone, "todayInZone");

// ../src/engine/seed.ts
function fnv1a32(input) {
  let hash = 2166136261;
  for (let i = 0; i < input.length; i++) {
    hash ^= input.charCodeAt(i);
    hash = Math.imul(hash, 16777619);
  }
  return hash >>> 0;
}
__name(fnv1a32, "fnv1a32");
function fmix32(h) {
  h ^= h >>> 16;
  h = Math.imul(h, 2246822507);
  h ^= h >>> 13;
  h = Math.imul(h, 3266489909);
  h ^= h >>> 16;
  return h >>> 0;
}
__name(fmix32, "fmix32");
function lotteryValue(seed, memberId) {
  const h = fmix32(fnv1a32(`${seed}\0${memberId}`));
  return h / 4294967296;
}
__name(lotteryValue, "lotteryValue");
function newSeed(random = Math.random) {
  const alphabet = "abcdefghijklmnopqrstuvwxyz0123456789";
  let out = "";
  for (let i = 0; i < 16; i++) out += alphabet[Math.floor(random() * alphabet.length)];
  return out;
}
__name(newSeed, "newSeed");

// ../src/engine/flow.ts
var MinCostFlow = class {
  static {
    __name(this, "MinCostFlow");
  }
  graph;
  constructor(nodeCount) {
    this.graph = Array.from({ length: nodeCount }, () => []);
  }
  addEdge(from, to, cap, cost) {
    this.graph[from].push({ to, rev: this.graph[to].length, cap, cost });
    this.graph[to].push({ to: from, rev: this.graph[from].length - 1, cap: 0, cost: -cost });
  }
  /** Runs min-cost max-flow from source to sink. */
  run(source, sink) {
    const n = this.graph.length;
    let flow = 0;
    let cost = 0;
    for (; ; ) {
      const dist = new Array(n).fill(Number.POSITIVE_INFINITY);
      const inQueue = new Array(n).fill(false);
      const prevNode = new Array(n).fill(-1);
      const prevEdge = new Array(n).fill(-1);
      dist[source] = 0;
      const queue = [source];
      inQueue[source] = true;
      while (queue.length) {
        const u = queue.shift();
        inQueue[u] = false;
        const edges = this.graph[u];
        for (let i = 0; i < edges.length; i++) {
          const e = edges[i];
          if (e.cap > 0 && dist[u] + e.cost < dist[e.to]) {
            dist[e.to] = dist[u] + e.cost;
            prevNode[e.to] = u;
            prevEdge[e.to] = i;
            if (!inQueue[e.to]) {
              inQueue[e.to] = true;
              queue.push(e.to);
            }
          }
        }
      }
      if (dist[sink] === Number.POSITIVE_INFINITY) break;
      let add = Number.POSITIVE_INFINITY;
      for (let v = sink; v !== source; v = prevNode[v]) {
        const e = this.graph[prevNode[v]][prevEdge[v]];
        add = Math.min(add, e.cap);
      }
      for (let v = sink; v !== source; v = prevNode[v]) {
        const e = this.graph[prevNode[v]][prevEdge[v]];
        e.cap -= add;
        this.graph[v][e.rev].cap += add;
      }
      flow += add;
      cost += add * dist[sink];
    }
    return { flow, cost };
  }
  /** Residual capacity of the reverse edge equals the flow sent on the forward edge. */
  flowOn(from, edgeIndex) {
    const e = this.graph[from][edgeIndex];
    return this.graph[e.to][e.rev].cap;
  }
  outEdges(from) {
    return this.graph[from];
  }
};

// ../src/engine/suggest.ts
var ALGORITHM_VERSION = "rotation-v1";
function allowedTeamIds(choice, teams) {
  const sorted = [...teams];
  switch (choice) {
    case "team1":
      return sorted[0] ? [sorted[0].id] : [];
    case "team2":
      return sorted[1] ? [sorted[1].id] : [];
    case "either":
      return sorted.map((t) => t.id);
    case "unavailable":
    default:
      return [];
  }
}
__name(allowedTeamIds, "allowedTeamIds");
function describeChoice(choice, teams) {
  switch (choice) {
    case "team1":
      return `${teams[0]?.name ?? "Team 1"} (${teams[0]?.local_time ?? ""}) only`;
    case "team2":
      return `${teams[1]?.name ?? "Team 2"} (${teams[1]?.local_time ?? ""}) only`;
    case "either":
      return "Either time";
    default:
      return "Unavailable";
  }
}
__name(describeChoice, "describeChoice");
function compareCandidates(a, b) {
  if (a.history.played_count !== b.history.played_count) return a.history.played_count - b.history.played_count;
  if (a.history.eligible_benches !== b.history.eligible_benches) return b.history.eligible_benches - a.history.eligible_benches;
  const la = a.history.last_played_at;
  const lb = b.history.last_played_at;
  if (la !== lb) {
    if (la === null) return -1;
    if (lb === null) return 1;
    return la < lb ? -1 : 1;
  }
  if (a.lottery !== b.lottery) return a.lottery - b.lottery;
  return a.member.id < b.member.id ? -1 : a.member.id > b.member.id ? 1 : 0;
}
__name(compareCandidates, "compareCandidates");
function round1(n) {
  return Math.round(n * 10) / 10;
}
__name(round1, "round1");
function reliabilityNote(h) {
  const parts = [];
  if (h.no_show_count) parts.push(`${h.no_show_count} no-show${h.no_show_count > 1 ? "s" : ""}`);
  if (h.withdrew_count) parts.push(`${h.withdrew_count} withdrawal${h.withdrew_count > 1 ? "s" : ""}`);
  if (h.unknown_count) parts.push(`${h.unknown_count} unknown`);
  return parts.length ? `Reliability: ${parts.join(", ")} in recent events` : null;
}
__name(reliabilityNote, "reliabilityNote");
function historyPhrase(h) {
  const plays = `${h.played_count} play${h.played_count === 1 ? "" : "s"} in last ${h.events_in_window} finalized`;
  const bench = h.eligible_benches ? `, benched ${h.eligible_benches}\xD7 while available` : "";
  const last = h.last_played_at ? `, last played ${h.last_played_at}` : ", never recorded";
  const incomplete = h.history_incomplete ? " \xB7 history incomplete" : "";
  return `${plays}${bench}${last}${incomplete}`;
}
__name(historyPhrase, "historyPhrase");
function generateSuggestions(input) {
  const { event, members } = input;
  const teams = [...event.teams];
  const seed = event.selection_seed;
  const errors = [];
  const warnings = [];
  const balance = input.balancePower ?? true;
  const memberById = new Map(members.map((m) => [m.id, m]));
  const capacityLeft = {};
  for (const t of teams) capacityLeft[t.id] = t.capacity;
  const locks = input.locks ?? [];
  const lockedIds = /* @__PURE__ */ new Set();
  const lockedAssignments = [];
  for (const lock of locks) {
    const member = memberById.get(lock.member_id);
    const team = teams.find((t) => t.id === lock.team_id);
    const name = member?.username ?? lock.member_id;
    if (!team) {
      errors.push({ code: "lock_unknown_team", member_id: lock.member_id, team_id: lock.team_id, message: `${name} is locked to a team that no longer exists.` });
      continue;
    }
    if (!member || !member.active) {
      errors.push({ code: "lock_inactive", member_id: lock.member_id, team_id: lock.team_id, message: `${name} is locked but is not an active member.` });
      continue;
    }
    if (lockedIds.has(lock.member_id)) {
      errors.push({ code: "lock_duplicate", member_id: lock.member_id, team_id: lock.team_id, message: `${name} is locked to more than one team.` });
      continue;
    }
    const availability = event.availability[lock.member_id];
    const allowed = availability ? allowedTeamIds(availability.choice, teams) : [];
    if (!allowed.includes(team.id)) {
      errors.push({
        code: "lock_unavailable",
        member_id: lock.member_id,
        team_id: lock.team_id,
        message: `${name} is locked to ${team.name} (${team.local_time}) but is ${availability ? describeChoice(availability.choice, teams).toLowerCase() : "no response (unknown)"}. Unlock or update availability.`
      });
      continue;
    }
    if (capacityLeft[team.id] <= 0) {
      errors.push({ code: "lock_capacity", member_id: lock.member_id, team_id: lock.team_id, message: `${team.name} already has ${team.capacity} locked players; ${name} cannot be locked there.` });
      continue;
    }
    capacityLeft[team.id]--;
    lockedIds.add(lock.member_id);
    lockedAssignments.push({
      event_id: event.id,
      member_id: lock.member_id,
      team_id: team.id,
      role: "starter",
      locked: true,
      lock_reason: lock.reason,
      locked_by: lock.locked_by,
      locked_at: lock.locked_at,
      reason: `Leader lock: ${lock.reason}`,
      revision: event.revision
    });
  }
  const candidates = [];
  for (const member of members) {
    if (!member.active) continue;
    if (lockedIds.has(member.id)) continue;
    const availability = event.availability[member.id];
    if (!availability) continue;
    const allowed = allowedTeamIds(availability.choice, teams);
    if (allowed.length === 0) continue;
    candidates.push({
      member,
      allowed,
      history: input.history[member.id] ?? emptyHistory(member),
      lottery: lotteryValue(seed, member.id)
    });
  }
  candidates.sort(compareCandidates);
  const SOURCE = 0;
  const SINK = 1;
  const candNode = /* @__PURE__ */ __name((i) => 2 + i, "candNode");
  const teamNode = /* @__PURE__ */ __name((j) => 2 + candidates.length + j, "teamNode");
  const flow = new MinCostFlow(2 + candidates.length + teams.length);
  const candEdgeIndex = [];
  candidates.forEach((c, i) => {
    candEdgeIndex.push(flow.outEdges(SOURCE).length);
    flow.addEdge(SOURCE, candNode(i), 1, i + 1);
    c.allowed.forEach((teamId) => {
      const j = teams.findIndex((t) => t.id === teamId);
      flow.addEdge(candNode(i), teamNode(j), 1, 0);
    });
  });
  teams.forEach((t, j) => flow.addEdge(teamNode(j), SINK, Math.max(0, capacityLeft[t.id]), 0));
  flow.run(SOURCE, SINK);
  const placement = /* @__PURE__ */ new Map();
  candidates.forEach((c, i) => {
    for (const e of flow.outEdges(candNode(i))) {
      const j = e.to - teamNode(0);
      if (j >= 0 && j < teams.length && e.cost === 0) {
        const rev = flow.outEdges(e.to)[e.rev];
        if (rev.cap > 0) placement.set(c.member.id, teams[j].id);
      }
    }
  });
  if (balance && teams.length === 2) {
    rebalance(candidates, placement, teams, capacityLeft, lockedAssignments, memberById);
  }
  const teamPower = {};
  const filled = {};
  for (const t of teams) {
    teamPower[t.id] = 0;
    filled[t.id] = 0;
  }
  for (const a of lockedAssignments) {
    if (a.team_id) {
      teamPower[a.team_id] += memberById.get(a.member_id)?.arena_power_m ?? 0;
      filled[a.team_id]++;
    }
  }
  const assignments = [...lockedAssignments];
  const views = [];
  for (const a of lockedAssignments) {
    const member = memberById.get(a.member_id);
    const h = input.history[member.id] ?? emptyHistory(member);
    views.push({
      member_id: member.id,
      username: member.username,
      arena_power_m: member.arena_power_m,
      allowed_team_ids: allowedTeamIds(event.availability[member.id].choice, teams),
      rank: 0,
      lottery: lotteryValue(seed, member.id),
      history: h,
      decision: "locked",
      team_id: a.team_id,
      reason: a.reason,
      reliability_note: reliabilityNote(h)
    });
  }
  const selectedByTeam = {};
  for (const t of teams) selectedByTeam[t.id] = [];
  candidates.forEach((c) => {
    const teamId = placement.get(c.member.id);
    if (teamId) selectedByTeam[teamId].push(c);
  });
  candidates.forEach((c, i) => {
    const teamId = placement.get(c.member.id) ?? null;
    const h = c.history;
    if (teamId) {
      teamPower[teamId] += c.member.arena_power_m;
      filled[teamId]++;
      const reason = selectionReason(c, candidates, placement, teams);
      assignments.push({
        event_id: event.id,
        member_id: c.member.id,
        team_id: teamId,
        role: "starter",
        locked: false,
        reason,
        revision: event.revision
      });
      views.push({
        member_id: c.member.id,
        username: c.member.username,
        arena_power_m: c.member.arena_power_m,
        allowed_team_ids: c.allowed,
        rank: i + 1,
        lottery: c.lottery,
        history: h,
        decision: "selected",
        team_id: teamId,
        reason,
        reliability_note: reliabilityNote(h)
      });
    } else {
      const reason = waitingReason(c, candidates, placement, teams, capacityLeft);
      const reserveTeam = c.allowed.length === 1 ? c.allowed[0] : null;
      assignments.push({
        event_id: event.id,
        member_id: c.member.id,
        team_id: reserveTeam,
        role: "reserve",
        locked: false,
        reason,
        revision: event.revision
      });
      views.push({
        member_id: c.member.id,
        username: c.member.username,
        arena_power_m: c.member.arena_power_m,
        allowed_team_ids: c.allowed,
        rank: i + 1,
        lottery: c.lottery,
        history: h,
        decision: "waiting",
        team_id: reserveTeam,
        reason,
        reliability_note: reliabilityNote(h)
      });
    }
  });
  const openSlots = {};
  for (const t of teams) {
    openSlots[t.id] = t.capacity - filled[t.id];
    teamPower[t.id] = round1(teamPower[t.id]);
    if (openSlots[t.id] > 0) {
      const waitingForOther = candidates.filter((c) => !placement.has(c.member.id)).length;
      if (waitingForOther > 0) {
        warnings.push(`${t.name} (${t.local_time}) has ${openSlots[t.id]} open slot${openSlots[t.id] === 1 ? "" : "s"}; ${waitingForOther} waiting player${waitingForOther === 1 ? " is" : "s are"} only available for the other time.`);
      } else {
        warnings.push(`${t.name} (${t.local_time}) has ${openSlots[t.id]} open slot${openSlots[t.id] === 1 ? "" : "s"} because not enough players are available.`);
      }
    }
  }
  const totalCapacity = teams.reduce((s, t) => s + t.capacity, 0);
  const eligible = candidates.length + lockedAssignments.length;
  if (eligible < totalCapacity) {
    warnings.push(`Only ${eligible} eligible players for ${totalCapacity} slots. Members with no response are treated as unknown, not available.`);
  }
  const incomplete = views.filter((v) => v.history.history_incomplete).length;
  if (incomplete > 0) {
    warnings.push(`${incomplete} candidate${incomplete === 1 ? " has" : "s have"} incomplete Canyon history (fewer than 8 finalized events tracked).`);
  }
  return {
    ok: errors.length === 0,
    algorithm_version: ALGORITHM_VERSION,
    seed,
    errors,
    warnings,
    assignments: errors.length ? [] : assignments,
    candidates: views,
    team_power: teamPower,
    open_slots: openSlots,
    eligible_count: eligible
  };
}
__name(generateSuggestions, "generateSuggestions");
function selectionReason(c, all, placement, teams) {
  const waiting = all.filter((o) => !placement.has(o.member.id));
  const h = c.history;
  let lead = "";
  if (h.played_count === 0 && h.events_in_window === 0) lead = "No Canyon history yet";
  else if (h.played_count === 0) lead = "No recent plays";
  else if (waiting.some((w) => w.history.played_count > h.played_count)) lead = "Fewer recent plays than waiting players";
  else if (waiting.some((w) => w.history.eligible_benches < h.eligible_benches)) lead = "Waited while available more often";
  else if (waiting.length && h.last_played_at) lead = `Waiting since ${h.last_played_at}`;
  else if (waiting.length === 0) lead = "Everyone available fits";
  else lead = "Won seeded tie-break";
  const team = teams.find((t) => t.id === placement.get(c.member.id));
  const flex = c.allowed.length > 1 && team ? ` \xB7 flexible, placed in ${team.name}` : "";
  return `${lead} \xB7 ${historyPhrase(h)}${flex}`;
}
__name(selectionReason, "selectionReason");
function waitingReason(c, all, placement, teams, capacityLeft) {
  const h = c.history;
  const allowedTeams = teams.filter((t) => c.allowed.includes(t.id));
  let lead;
  if (allowedTeams.length === 1) {
    const t = allowedTeams[0];
    const other = teams.find((o) => o.id !== t.id);
    const otherHasRoom = other ? countPlaced(placement, other.id) < capacityLeft[other.id] : false;
    lead = otherHasRoom ? `Only available for ${t.local_time}, which is full; ${other.local_time} still has open slots` : `${t.name} (${t.local_time}) is full`;
  } else {
    lead = "Both teams are full";
  }
  const selectedAhead = all.filter((o) => placement.has(o.member.id) && compareCandidates(o, c) < 0).length;
  const detail = selectedAhead ? `; ${selectedAhead} selected player${selectedAhead === 1 ? " has" : "s have"} equal or higher priority` : "";
  return `${lead}${detail} \xB7 ${historyPhrase(h)}`;
}
__name(waitingReason, "waitingReason");
function countPlaced(placement, teamId) {
  let n = 0;
  for (const v of placement.values()) if (v === teamId) n++;
  return n;
}
__name(countPlaced, "countPlaced");
function rebalance(candidates, placement, teams, capacityLeft, locked, memberById) {
  const [t1, t2] = teams;
  const power = /* @__PURE__ */ __name((id) => memberById.get(id)?.arena_power_m ?? 0, "power");
  const total = { [t1.id]: 0, [t2.id]: 0 };
  const count = { [t1.id]: 0, [t2.id]: 0 };
  for (const a of locked) if (a.team_id) total[a.team_id] += power(a.member_id);
  for (const [id, team] of placement) {
    total[team] += power(id);
    count[team]++;
  }
  const flexible = candidates.filter((c) => c.allowed.length === 2 && placement.has(c.member.id));
  const gap = /* @__PURE__ */ __name(() => Math.abs(total[t1.id] - total[t2.id]), "gap");
  for (let iter = 0; iter < 200; iter++) {
    let best = null;
    const current = gap();
    for (const c of flexible) {
      const from = placement.get(c.member.id);
      const to = from === t1.id ? t2.id : t1.id;
      if (count[to] >= capacityLeft[to]) continue;
      const p = power(c.member.id);
      const g = Math.abs(total[t1.id] - total[t2.id] + (from === t1.id ? -2 * p : 2 * p));
      if (g < current - 1e-9 && (!best || g < best.gap - 1e-9)) {
        best = {
          gap: g,
          apply: /* @__PURE__ */ __name(() => {
            placement.set(c.member.id, to);
            total[from] -= p;
            total[to] += p;
            count[from]--;
            count[to]++;
          }, "apply")
        };
      }
    }
    for (let i = 0; i < flexible.length; i++) {
      for (let j = i + 1; j < flexible.length; j++) {
        const a = flexible[i];
        const b = flexible[j];
        const ta = placement.get(a.member.id);
        const tb = placement.get(b.member.id);
        if (ta === tb) continue;
        const pa = power(a.member.id);
        const pb = power(b.member.id);
        const delta = pa - pb;
        const t1Delta = ta === t1.id ? -delta : delta;
        const g = Math.abs(total[t1.id] + t1Delta - (total[t2.id] - t1Delta));
        if (g < current - 1e-9 && (!best || g < best.gap - 1e-9)) {
          best = {
            gap: g,
            apply: /* @__PURE__ */ __name(() => {
              placement.set(a.member.id, tb);
              placement.set(b.member.id, ta);
              total[ta] += pb - pa;
              total[tb] += pa - pb;
            }, "apply")
          };
        }
      }
    }
    if (!best) break;
    best.apply();
  }
}
__name(rebalance, "rebalance");

// ../src/engine/lifecycle.ts
var LifecycleError = class extends Error {
  static {
    __name(this, "LifecycleError");
  }
  code;
  constructor(code, message) {
    super(message);
    this.code = code;
  }
};
var auditCounter = 0;
function audit(ctx, event_id, action, before, after, reason) {
  auditCounter++;
  return { id: `${ctx.now}-${auditCounter}`, event_id, actor_id: ctx.actor, action, before, after, reason, timestamp: ctx.now };
}
__name(audit, "audit");
function createDraftEvent(opts) {
  if (weekday(opts.date) !== 5) {
    throw new LifecycleError("not_friday", `Canyon Clash must be on a Friday; ${opts.date} is not.`);
  }
  const id = eventIdFor(opts.series_id, opts.date);
  const times = opts.team_times ?? { team1: "18:00", team2: "23:00" };
  const cap = opts.capacity ?? 20;
  const teams = [
    { id: `${id}:team1`, event_id: id, name: "Team 1", local_time: times.team1, capacity: cap },
    { id: `${id}:team2`, event_id: id, name: "Team 2", local_time: times.team2, capacity: cap }
  ];
  return {
    id,
    series_id: opts.series_id,
    date: opts.date,
    timezone: opts.timezone,
    date_confirmed: opts.date_confirmed ?? false,
    status: "draft",
    algorithm_version: ALGORITHM_VERSION,
    selection_seed: opts.seed ?? newSeed(),
    revision: 1,
    teams,
    availability: {},
    assignments: [],
    attendance: {},
    published_revisions: [],
    confirmations: {},
    finalized_at: null,
    finalized_by: null
  };
}
__name(createDraftEvent, "createDraftEvent");
function ensureNextWeekDraft(events, opts) {
  const date = nextFriday(opts.afterDate);
  const id = eventIdFor(opts.series_id, date);
  const existing = events.find((e) => e.id === id);
  if (existing) return { event: existing, created: false };
  return { event: createDraftEvent({ ...opts, date }), created: true };
}
__name(ensureNextWeekDraft, "ensureNextWeekDraft");
function assertEditable(event, allowPublished = true) {
  if (event.status === "finalized") throw new LifecycleError("finalized", "This event is finalized. Use attendance corrections instead.");
  if (event.status === "canceled") throw new LifecycleError("canceled", "This event is canceled.");
  if (!allowPublished && event.status === "published") throw new LifecycleError("published", "This lineup is published; edits create a new revision.");
}
__name(assertEditable, "assertEditable");
function assertRevision(event, expected2) {
  if (expected2 !== void 0 && expected2 !== event.revision) {
    throw new LifecycleError("stale_revision", `Someone else changed this event (revision ${event.revision}, you had ${expected2}). Reload to compare.`);
  }
}
__name(assertRevision, "assertRevision");
function setAvailability(event, memberId, choice, ctx, recordedBy = "self") {
  assertEditable(event);
  const before = event.availability[memberId] ?? null;
  const next = { event_id: event.id, member_id: memberId, choice, recorded_by: recordedBy, updated_at: ctx.now };
  const availability = { ...event.availability, [memberId]: next };
  let assignments = event.assignments;
  const allowed = allowedTeamIds(choice, event.teams);
  const current = assignments.find((a) => a.member_id === memberId && a.role === "starter");
  if (current && current.team_id && !allowed.includes(current.team_id)) {
    assignments = assignments.filter((a) => a.member_id !== memberId);
  }
  return {
    event: { ...event, availability, assignments },
    audit: [audit(ctx, event.id, "availability.set", before, next)]
  };
}
__name(setAvailability, "setAvailability");
function currentLocks(event) {
  return event.assignments.filter((a) => a.locked && a.role === "starter" && a.team_id).map((a) => ({ member_id: a.member_id, team_id: a.team_id, reason: a.lock_reason ?? "Leader lock", locked_by: a.locked_by, locked_at: a.locked_at }));
}
__name(currentLocks, "currentLocks");
function suggest(event, members, allEvents) {
  const history = computeHistory(allEvents.filter((e) => e.id !== event.id), members);
  return generateSuggestions({ event, members, history, locks: currentLocks(event) });
}
__name(suggest, "suggest");
function applySuggestions(event, members, allEvents, ctx, expectedRevision) {
  assertEditable(event);
  assertRevision(event, expectedRevision);
  const result = suggest(event, members, allEvents);
  if (!result.ok) {
    throw new LifecycleError("locks_conflict", result.errors.map((e) => e.message).join(" "));
  }
  const next = { ...event, assignments: result.assignments, revision: event.revision + 1 };
  return { event: next, result, audit: [audit(ctx, event.id, "suggestions.generate", summarize(event.assignments), summarize(next.assignments))] };
}
__name(applySuggestions, "applySuggestions");
function summarize(assignments) {
  const out = {};
  for (const a of assignments) {
    const key = a.role === "starter" ? a.team_id ?? "starter" : "reserve";
    (out[key] ??= []).push(a.member_id);
  }
  return out;
}
__name(summarize, "summarize");
function starters(event, teamId) {
  return event.assignments.filter((a) => a.role === "starter" && a.team_id === teamId);
}
__name(starters, "starters");
function memberAllowed(event, memberId, teamId) {
  const av = event.availability[memberId];
  return !!av && allowedTeamIds(av.choice, event.teams).includes(teamId);
}
__name(memberAllowed, "memberAllowed");
function replaceAssignment(list, next) {
  const without = list.filter((a) => a.member_id !== next.member_id);
  return [...without, next];
}
__name(replaceAssignment, "replaceAssignment");
function lockMember(event, memberId, teamId, reason, ctx, expectedRevision) {
  assertEditable(event);
  assertRevision(event, expectedRevision);
  if (!reason.trim()) throw new LifecycleError("reason_required", "A short reason is required to lock a player.");
  if (!memberAllowed(event, memberId, teamId)) throw new LifecycleError("unavailable", "This member is not available for that team time.");
  const team = event.teams.find((t) => t.id === teamId);
  const existing = event.assignments.find((a) => a.member_id === memberId);
  const others = starters(event, teamId).filter((a) => a.member_id !== memberId);
  if (others.length >= team.capacity) throw new LifecycleError("capacity", `${team.name} is full (${team.capacity}/${team.capacity}). Swap with a player instead.`);
  const next = {
    event_id: event.id,
    member_id: memberId,
    team_id: teamId,
    role: "starter",
    locked: true,
    lock_reason: reason.trim(),
    locked_by: ctx.actor === "system" ? void 0 : ctx.actor,
    locked_at: ctx.now,
    reason: `Leader lock: ${reason.trim()}`,
    revision: event.revision + 1
  };
  return {
    event: { ...event, assignments: replaceAssignment(event.assignments, next), revision: event.revision + 1 },
    audit: [audit(ctx, event.id, "assignment.lock", existing ?? null, next, reason)]
  };
}
__name(lockMember, "lockMember");
function unlockMember(event, memberId, ctx, expectedRevision) {
  assertEditable(event);
  assertRevision(event, expectedRevision);
  const existing = event.assignments.find((a) => a.member_id === memberId);
  if (!existing || !existing.locked) throw new LifecycleError("not_locked", "This member is not locked.");
  const next = { ...existing, locked: false, lock_reason: void 0, locked_by: void 0, locked_at: void 0, reason: "Unlocked by leader; kept in team until regenerated", revision: event.revision + 1 };
  return {
    event: { ...event, assignments: replaceAssignment(event.assignments, next), revision: event.revision + 1 },
    audit: [audit(ctx, event.id, "assignment.unlock", existing, next)]
  };
}
__name(unlockMember, "unlockMember");
function moveMember(event, memberId, target, ctx, expectedRevision) {
  assertEditable(event);
  assertRevision(event, expectedRevision);
  const existing = event.assignments.find((a) => a.member_id === memberId) ?? null;
  let next;
  if (target === "reserve") {
    const av = event.availability[memberId];
    const allowed = av ? allowedTeamIds(av.choice, event.teams) : [];
    if (!allowed.length) throw new LifecycleError("unavailable", "This member has not marked themselves available.");
    next = {
      event_id: event.id,
      member_id: memberId,
      team_id: allowed.length === 1 ? allowed[0] : null,
      role: "reserve",
      locked: false,
      reason: "Moved to reserves by leader",
      revision: event.revision + 1
    };
  } else {
    if (!memberAllowed(event, memberId, target)) throw new LifecycleError("unavailable", "This member is not available for that team time.");
    const team = event.teams.find((t) => t.id === target);
    const others = starters(event, target).filter((a) => a.member_id !== memberId);
    if (others.length >= team.capacity) throw new LifecycleError("capacity", `${team.name} is full (${team.capacity}/${team.capacity}). Use swap instead.`);
    next = {
      event_id: event.id,
      member_id: memberId,
      team_id: target,
      role: "starter",
      locked: existing?.locked ?? false,
      lock_reason: existing?.lock_reason,
      locked_by: existing?.locked_by,
      locked_at: existing?.locked_at,
      reason: existing?.locked ? existing.reason : `Moved to ${team.name} by leader`,
      revision: event.revision + 1
    };
  }
  return {
    event: { ...event, assignments: replaceAssignment(event.assignments, next), revision: event.revision + 1 },
    audit: [audit(ctx, event.id, "assignment.move", existing, next)]
  };
}
__name(moveMember, "moveMember");
function swapMembers(event, aId, bId, ctx, expectedRevision) {
  assertEditable(event);
  assertRevision(event, expectedRevision);
  if (aId === bId) throw new LifecycleError("same_member", "Choose two different players.");
  const a = event.assignments.find((x) => x.member_id === aId);
  const b = event.assignments.find((x) => x.member_id === bId);
  if (!a || !b) throw new LifecycleError("not_assigned", "Both players must be in the lineup or reserves.");
  const targetForA = b.role === "starter" ? b.team_id : "reserve";
  const targetForB = a.role === "starter" ? a.team_id : "reserve";
  if (targetForA !== "reserve" && targetForA && !memberAllowed(event, aId, targetForA)) {
    throw new LifecycleError("unavailable", `${aId} is not available for the other team's time.`);
  }
  if (targetForB !== "reserve" && targetForB && !memberAllowed(event, bId, targetForB)) {
    throw new LifecycleError("unavailable", `${bId} is not available for the other team's time.`);
  }
  const rev = event.revision + 1;
  const nextA = { ...a, team_id: b.team_id, role: b.role, reason: `Swapped with ${bId} by leader`, revision: rev };
  const nextB = { ...b, team_id: a.team_id, role: a.role, reason: `Swapped with ${aId} by leader`, revision: rev };
  if (nextA.role === "reserve" && nextA.team_id === null) {
    const av = event.availability[aId];
    nextA.team_id = av && allowedTeamIds(av.choice, event.teams).length === 1 ? allowedTeamIds(av.choice, event.teams)[0] : null;
  }
  if (nextB.role === "reserve" && nextB.team_id === null) {
    const av = event.availability[bId];
    nextB.team_id = av && allowedTeamIds(av.choice, event.teams).length === 1 ? allowedTeamIds(av.choice, event.teams)[0] : null;
  }
  const assignments = replaceAssignment(replaceAssignment(event.assignments, nextA), nextB);
  return {
    event: { ...event, assignments, revision: rev },
    audit: [audit(ctx, event.id, "assignment.swap", [a, b], [nextA, nextB])]
  };
}
__name(swapMembers, "swapMembers");
function publishBlockers(event) {
  const out = [];
  if (!event.timezone || !isValidTimeZone(event.timezone)) out.push({ code: "timezone", message: "Set the event timezone before publishing." });
  if (!event.date_confirmed) out.push({ code: "date", message: "Confirm the event date before publishing." });
  if (event.status === "finalized" || event.status === "canceled") out.push({ code: "status", message: "This event can no longer be published." });
  for (const t of event.teams) {
    if (starters(event, t.id).length > t.capacity) out.push({ code: "capacity", message: `${t.name} exceeds ${t.capacity} starters.` });
  }
  const seen = /* @__PURE__ */ new Set();
  for (const a of event.assignments) {
    if (seen.has(a.member_id)) out.push({ code: "duplicate", message: `${a.member_id} appears twice in the lineup.` });
    seen.add(a.member_id);
    if (a.role === "starter" && a.team_id && !memberAllowed(event, a.member_id, a.team_id)) {
      out.push({ code: "unavailable", message: `${a.member_id} is assigned to a time they are not available for.` });
    }
  }
  if (!event.assignments.some((a) => a.role === "starter")) out.push({ code: "empty", message: "Generate or assign at least one starter first." });
  return out;
}
__name(publishBlockers, "publishBlockers");
function publishEvent(event, ctx, expectedRevision) {
  assertRevision(event, expectedRevision);
  const blockers = publishBlockers(event);
  if (blockers.length) throw new LifecycleError("blocked", blockers.map((b) => b.message).join(" "));
  const rev = event.revision + 1;
  const snapshot = event.assignments.map((a) => ({ ...a, revision: rev }));
  const published = { revision: rev, published_at: ctx.now, published_by: ctx.actor === "system" ? "system" : ctx.actor, assignments: snapshot };
  const next = {
    ...event,
    status: "published",
    revision: rev,
    assignments: snapshot,
    published_revisions: [...event.published_revisions, published],
    // Confirmations from earlier revisions are invalidated on republish.
    confirmations: {}
  };
  return { event: next, audit: [audit(ctx, event.id, "lineup.publish", { revision: event.revision }, { revision: rev, starters: snapshot.filter((a) => a.role === "starter").length })] };
}
__name(publishEvent, "publishEvent");
function confirmAssignment(event, memberId, ctx) {
  if (event.status !== "published") throw new LifecycleError("not_published", "Nothing has been published yet.");
  const a = event.assignments.find((x) => x.member_id === memberId);
  if (!a) throw new LifecycleError("not_assigned", "You are not in this lineup.");
  const confirmations = { ...event.confirmations, [memberId]: { revision: event.revision, confirmed_at: ctx.now } };
  return { event: { ...event, confirmations }, audit: [audit(ctx, event.id, "assignment.confirm", null, { member_id: memberId, revision: event.revision })] };
}
__name(confirmAssignment, "confirmAssignment");
function recordAttendance(event, memberId, outcome, ctx, opts = {}) {
  if (event.status !== "published" && event.status !== "finalized") {
    throw new LifecycleError("not_published", "Publish the lineup before recording attendance.");
  }
  const before = event.attendance[memberId] ?? null;
  const assignment = event.assignments.find((a) => a.member_id === memberId);
  const teamId = opts.team_id !== void 0 ? opts.team_id : assignment?.team_id ?? before?.team_id ?? null;
  const substitute = opts.substitute ?? before?.substitute ?? (assignment ? assignment.role !== "starter" : true);
  const next = {
    event_id: event.id,
    member_id: memberId,
    team_id: teamId,
    outcome,
    confirmed_by: ctx.actor === "system" ? null : ctx.actor,
    confirmed_at: outcome === "unknown" ? null : ctx.now,
    substitute
  };
  const attendance = { ...event.attendance, [memberId]: next };
  return { event: { ...event, attendance }, audit: [audit(ctx, event.id, event.status === "finalized" ? "attendance.correct" : "attendance.record", before, next)] };
}
__name(recordAttendance, "recordAttendance");
function attendanceRows(event) {
  const rows = /* @__PURE__ */ new Map();
  for (const a of event.assignments) {
    rows.set(a.member_id, {
      event_id: event.id,
      member_id: a.member_id,
      team_id: a.team_id,
      outcome: "unknown",
      confirmed_by: null,
      confirmed_at: null,
      substitute: a.role !== "starter"
    });
  }
  for (const att of Object.values(event.attendance)) rows.set(att.member_id, att);
  return [...rows.values()];
}
__name(attendanceRows, "attendanceRows");
function finalizeEvent(event, ctx) {
  if (event.status === "finalized") return { event, audit: [] };
  if (event.status !== "published") throw new LifecycleError("not_published", "Publish the lineup and record attendance before finalizing.");
  const next = { ...event, status: "finalized", finalized_at: ctx.now, finalized_by: ctx.actor === "system" ? null : ctx.actor };
  const summary = attendanceSummary(next);
  return { event: next, audit: [audit(ctx, event.id, "attendance.finalize", null, summary)] };
}
__name(finalizeEvent, "finalizeEvent");
function attendanceSummary(event) {
  const out = { played: 0, no_show: 0, withdrew: 0, unused_reserve: 0, unknown: 0 };
  for (const row of attendanceRows(event)) out[row.outcome]++;
  return out;
}
__name(attendanceSummary, "attendanceSummary");
function cancelEvent(event, ctx, reason) {
  if (event.status === "finalized") throw new LifecycleError("finalized", "Finalized events cannot be canceled.");
  return { event: { ...event, status: "canceled", note: reason }, audit: [audit(ctx, event.id, "event.cancel", event.status, "canceled", reason)] };
}
__name(cancelEvent, "cancelEvent");
function updateSchedule(event, patch, ctx) {
  if (event.status === "finalized" || event.status === "canceled") throw new LifecycleError("finalized", "This event can no longer be rescheduled.");
  if (patch.date !== void 0 && weekday(patch.date) !== 5) throw new LifecycleError("not_friday", "Canyon Clash must be on a Friday in the event timezone.");
  if (patch.timezone !== void 0 && patch.timezone !== null && !isValidTimeZone(patch.timezone)) throw new LifecycleError("bad_timezone", "Unknown timezone name.");
  const teams = event.teams.map((t, i) => {
    const key = i === 0 ? "team1" : "team2";
    const time = patch.team_times?.[key];
    return time ? { ...t, local_time: time } : t;
  });
  const timesChanged = teams.some((t, i) => t.local_time !== event.teams[i].local_time);
  const next = {
    ...event,
    date: patch.date ?? event.date,
    timezone: patch.timezone !== void 0 ? patch.timezone : event.timezone,
    date_confirmed: patch.date_confirmed ?? (patch.date !== void 0 ? false : event.date_confirmed),
    teams,
    // A time change invalidates existing availability responses.
    availability: timesChanged ? {} : event.availability,
    assignments: timesChanged ? [] : event.assignments,
    confirmations: timesChanged ? {} : event.confirmations,
    revision: event.revision + 1
  };
  return { event: next, audit: [audit(ctx, event.id, "event.schedule", { date: event.date, timezone: event.timezone, teams: event.teams }, { date: next.date, timezone: next.timezone, teams: next.teams })] };
}
__name(updateSchedule, "updateSchedule");
function replaceAssignments(event, assignments, ctx, expectedRevision) {
  assertEditable(event);
  assertRevision(event, expectedRevision);
  const seen = /* @__PURE__ */ new Set();
  for (const a of assignments) {
    if (seen.has(a.member_id)) throw new LifecycleError("duplicate", "A member appears twice.");
    seen.add(a.member_id);
    if (a.role === "starter" && a.team_id && !memberAllowed(event, a.member_id, a.team_id)) {
      throw new LifecycleError("unavailable", "A restored assignment conflicts with current availability.");
    }
  }
  for (const t of event.teams) {
    if (assignments.filter((a) => a.role === "starter" && a.team_id === t.id).length > t.capacity) {
      throw new LifecycleError("capacity", `${t.name} would exceed ${t.capacity} starters.`);
    }
  }
  const rev = event.revision + 1;
  const next = assignments.map((a) => ({ ...a, revision: rev }));
  return { event: { ...event, assignments: next, revision: rev }, audit: [audit(ctx, event.id, "assignment.restore", summarize(event.assignments), summarize(next))] };
}
__name(replaceAssignments, "replaceAssignments");
function withMechanicalNote(member, note) {
  return { ...member, mechanical_notes: note.trim() ? note.trim() : void 0 };
}
__name(withMechanicalNote, "withMechanicalNote");

// ../src/engine/organization.ts
function slotValue(slot) {
  if (slot.member_id) return { kind: "member", member_id: slot.member_id };
  if (slot.placeholder) return { kind: "placeholder", label: slot.placeholder };
  if (slot.source_name) return { kind: "source", label: slot.source_name };
  return { kind: "empty" };
}
__name(slotValue, "slotValue");
function applyValue(slot, value) {
  switch (value.kind) {
    case "empty":
      return { ...slot, member_id: null, source_name: null, placeholder: null };
    case "member":
      return { ...slot, member_id: value.member_id, source_name: null, placeholder: null };
    case "placeholder":
      return { ...slot, member_id: null, source_name: value.label, placeholder: value.label };
    case "source":
      return { ...slot, member_id: null, source_name: value.label, placeholder: null };
  }
}
__name(applyValue, "applyValue");
var counter = 0;
function audit2(ctx, action, before, after) {
  counter++;
  return { id: `${ctx.now}-org-${counter}`, event_id: null, actor_id: ctx.actor, action, before, after, timestamp: ctx.now };
}
__name(audit2, "audit");
function assertRevision2(state, expected2) {
  if (expected2 !== void 0 && expected2 !== state.revision) {
    throw new LifecycleError("stale_revision", `Another leader changed the organization board (revision ${state.revision}, you had ${expected2}). Reload to compare.`);
  }
}
__name(assertRevision2, "assertRevision");
function assertNoDuplicate(task) {
  const seen = /* @__PURE__ */ new Set();
  for (const s of task.slots) {
    if (s.member_id) {
      if (seen.has(s.member_id)) throw new LifecycleError("duplicate", "The same member cannot hold two slots in one task.");
      seen.add(s.member_id);
    }
  }
}
__name(assertNoDuplicate, "assertNoDuplicate");
function applySlotEdits(state, edits, ctx, expectedRevision) {
  assertRevision2(state, expectedRevision);
  const inverse = [];
  const responsibilities = state.responsibilities.map((r) => ({ ...r, slots: r.slots.map((s) => ({ ...s })) }));
  const beforeSnapshot = [];
  for (const edit of edits) {
    const task = responsibilities.find((r) => r.id === edit.responsibility_id);
    if (!task) throw new LifecycleError("not_found", "Task not found.");
    const idx = task.slots.findIndex((s) => s.position === edit.position);
    if (idx < 0) throw new LifecycleError("not_found", "Slot not found.");
    const before = slotValue(task.slots[idx]);
    beforeSnapshot.push({ responsibility_id: task.id, position: edit.position, value: before });
    task.slots[idx] = applyValue(task.slots[idx], edit.value);
  }
  for (const task of responsibilities) assertNoDuplicate(task);
  for (let i = beforeSnapshot.length - 1; i >= 0; i--) inverse.push(beforeSnapshot[i]);
  const next = { ...state, responsibilities, revision: state.revision + 1 };
  return { state: next, inverse, audit: [audit2(ctx, "organization.slots", beforeSnapshot, edits)] };
}
__name(applySlotEdits, "applySlotEdits");
function addTask(state, title, ctx, expectedRevision) {
  assertRevision2(state, expectedRevision);
  if (!title.trim()) throw new LifecycleError("title_required", "Task title is required.");
  const id = `responsibility-${Date.now().toString(36)}-${Math.floor(Math.random() * 1e6).toString(36)}`;
  const labels = ["Lead", "Assigned 2", "Assigned 3", "Assigned 4"];
  const task = {
    id,
    title: title.trim(),
    order: state.responsibilities.length,
    archived: false,
    slots: labels.map((label, i) => ({ position: i + 1, label, source_name: null, member_id: null, placeholder: null }))
  };
  return { state: { ...state, responsibilities: [...state.responsibilities, task], revision: state.revision + 1 }, inverse: [], audit: [audit2(ctx, "organization.task.add", null, task)] };
}
__name(addTask, "addTask");
function renameTask(state, id, title, ctx, expectedRevision) {
  assertRevision2(state, expectedRevision);
  if (!title.trim()) throw new LifecycleError("title_required", "Task title is required.");
  const before = state.responsibilities.find((r) => r.id === id);
  if (!before) throw new LifecycleError("not_found", "Task not found.");
  const responsibilities = state.responsibilities.map((r) => r.id === id ? { ...r, title: title.trim() } : r);
  return { state: { ...state, responsibilities, revision: state.revision + 1 }, inverse: [], audit: [audit2(ctx, "organization.task.rename", before.title, title.trim())] };
}
__name(renameTask, "renameTask");
function setTaskArchived(state, id, archived, ctx, expectedRevision) {
  assertRevision2(state, expectedRevision);
  const responsibilities = state.responsibilities.map((r) => r.id === id ? { ...r, archived } : r);
  return { state: { ...state, responsibilities, revision: state.revision + 1 }, inverse: [], audit: [audit2(ctx, "organization.task.archive", !archived, archived)] };
}
__name(setTaskArchived, "setTaskArchived");
function reorderTask(state, id, direction, ctx, expectedRevision) {
  assertRevision2(state, expectedRevision);
  const sorted = [...state.responsibilities].sort((a, b) => a.order - b.order);
  const idx = sorted.findIndex((r) => r.id === id);
  const target = idx + direction;
  if (idx < 0 || target < 0 || target >= sorted.length) return { state, inverse: [], audit: [] };
  [sorted[idx], sorted[target]] = [sorted[target], sorted[idx]];
  const responsibilities = sorted.map((r, i) => ({ ...r, order: i }));
  return { state: { ...state, responsibilities, revision: state.revision + 1 }, inverse: [], audit: [audit2(ctx, "organization.task.reorder", idx, target)] };
}
__name(reorderTask, "reorderTask");
function setNameMapping(state, sourceName, memberId, ctx, expectedRevision) {
  assertRevision2(state, expectedRevision);
  const mapping = { ...state.name_mapping, [sourceName]: memberId };
  const responsibilities = state.responsibilities.map((r) => ({
    ...r,
    slots: r.slots.map((s) => s.source_name === sourceName && !s.placeholder && !s.member_id && memberId ? { ...s, member_id: memberId } : s)
  }));
  for (const task of responsibilities) assertNoDuplicate(task);
  return { state: { ...state, name_mapping: mapping, responsibilities, revision: state.revision + 1 }, inverse: [], audit: [audit2(ctx, "organization.mapping", { [sourceName]: state.name_mapping[sourceName] ?? null }, { [sourceName]: memberId })] };
}
__name(setNameMapping, "setNameMapping");

// src/env.ts
var HttpError = class extends Error {
  static {
    __name(this, "HttpError");
  }
  status;
  code;
  constructor(status, code, message) {
    super(message);
    this.status = status;
    this.code = code;
  }
};

// src/auth.ts
var SESSION_DAYS = 90;
var PBKDF2_ITERATIONS = 12e4;
function toBase64Url(bytes) {
  const arr = bytes instanceof Uint8Array ? bytes : new Uint8Array(bytes);
  let s = "";
  for (const b of arr) s += String.fromCharCode(b);
  return btoa(s).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}
__name(toBase64Url, "toBase64Url");
function fromBase64Url(s) {
  const b64 = s.replace(/-/g, "+").replace(/_/g, "/") + "=".repeat((4 - s.length % 4) % 4);
  const bin = atob(b64);
  const out = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; i++) out[i] = bin.charCodeAt(i);
  return out;
}
__name(fromBase64Url, "fromBase64Url");
function randomToken(bytes = 32) {
  const buf = new Uint8Array(bytes);
  crypto.getRandomValues(buf);
  return toBase64Url(buf);
}
__name(randomToken, "randomToken");
function newId(prefix) {
  return `${prefix}_${randomToken(12)}`;
}
__name(newId, "newId");
async function sha256(text) {
  const digest = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(text));
  return toBase64Url(digest);
}
__name(sha256, "sha256");
async function pbkdf2(password, salt) {
  const key = await crypto.subtle.importKey("raw", new TextEncoder().encode(password), "PBKDF2", false, ["deriveBits"]);
  const bits = await crypto.subtle.deriveBits({ name: "PBKDF2", hash: "SHA-256", salt, iterations: PBKDF2_ITERATIONS }, key, 256);
  return toBase64Url(bits);
}
__name(pbkdf2, "pbkdf2");
async function hashPassword(password) {
  const salt = new Uint8Array(16);
  crypto.getRandomValues(salt);
  return { hash: await pbkdf2(password, salt), salt: toBase64Url(salt) };
}
__name(hashPassword, "hashPassword");
async function verifyPassword(password, hash, salt) {
  const computed = await pbkdf2(password, fromBase64Url(salt));
  if (computed.length !== hash.length) return false;
  let diff = 0;
  for (let i = 0; i < computed.length; i++) diff |= computed.charCodeAt(i) ^ hash.charCodeAt(i);
  return diff === 0;
}
__name(verifyPassword, "verifyPassword");
function validateUsername(username) {
  const u = username.trim();
  if (!/^[A-Za-z0-9_.-]{3,32}$/.test(u)) throw new HttpError(400, "bad_username", "Username must be 3\u201332 letters, digits, dots, dashes or underscores.");
  return u;
}
__name(validateUsername, "validateUsername");
function validatePassword(password) {
  if (typeof password !== "string" || password.length < 8) throw new HttpError(400, "bad_password", "Password must be at least 8 characters.");
  if (password.length > 200) throw new HttpError(400, "bad_password", "Password is too long.");
}
__name(validatePassword, "validatePassword");
function rowToAccount(r) {
  return { id: r.id, username: r.username, role: r.role, member_id: r.member_id, verified: r.verified === 1, disabled: r.disabled === 1, created_at: r.created_at };
}
__name(rowToAccount, "rowToAccount");
async function createSession(env, accountId, now) {
  const token = randomToken(32);
  const expires = new Date(now.getTime() + SESSION_DAYS * 864e5).toISOString();
  await env.DB.prepare("INSERT INTO sessions (token_hash, account_id, created_at, expires_at) VALUES (?, ?, ?, ?)").bind(await sha256(token), accountId, now.toISOString(), expires).run();
  return token;
}
__name(createSession, "createSession");
async function deleteSession(env, token) {
  await env.DB.prepare("DELETE FROM sessions WHERE token_hash = ?").bind(await sha256(token)).run();
}
__name(deleteSession, "deleteSession");
async function accountFromRequest(env, request, now) {
  const header = request.headers.get("authorization") ?? "";
  const match = /^Bearer\s+(.+)$/i.exec(header);
  if (!match) return null;
  const row = await env.DB.prepare(
    `SELECT a.* FROM sessions s JOIN accounts a ON a.id = s.account_id WHERE s.token_hash = ? AND s.expires_at > ?`
  ).bind(await sha256(match[1].trim()), now.toISOString()).first();
  if (!row || row.disabled === 1) return null;
  return rowToAccount(row);
}
__name(accountFromRequest, "accountFromRequest");
async function findAccountByUsername(env, username) {
  return env.DB.prepare("SELECT * FROM accounts WHERE username = ? COLLATE NOCASE").bind(username).first();
}
__name(findAccountByUsername, "findAccountByUsername");
async function leaderCount(env) {
  const row = await env.DB.prepare(`SELECT COUNT(*) AS n FROM accounts WHERE role = 'leader' AND disabled = 0`).first();
  return row?.n ?? 0;
}
__name(leaderCount, "leaderCount");
function requireAccount(account) {
  if (!account) throw new HttpError(401, "unauthenticated", "Sign in to continue.");
  return account;
}
__name(requireAccount, "requireAccount");
function requireLeader(account) {
  const a = requireAccount(account);
  if (a.role !== "leader") throw new HttpError(403, "forbidden", "Leaders only.");
  return a;
}
__name(requireLeader, "requireLeader");

// src/db.ts
var DEFAULT_SETTINGS = { timezone: null, motion: "system", haptics: false, default_team_times: { team1: "18:00", team2: "23:00" } };
async function ensureSeeded(env, now) {
  const row = await env.DB.prepare("SELECT COUNT(*) AS n FROM members").first();
  if ((row?.n ?? 0) > 0) return;
  const iso = now.toISOString();
  const members = loadSeedMembers();
  const statements = members.map((m) => env.DB.prepare("INSERT OR IGNORE INTO members (id, doc, updated_at) VALUES (?, ?, ?)").bind(m.id, JSON.stringify(m), iso));
  const org = loadSeedOrganization();
  statements.push(env.DB.prepare("INSERT OR IGNORE INTO documents (key, revision, doc, updated_at) VALUES (?, ?, ?, ?)").bind("organization", org.revision, JSON.stringify(org), iso));
  statements.push(env.DB.prepare("INSERT OR IGNORE INTO documents (key, revision, doc, updated_at) VALUES (?, ?, ?, ?)").bind("settings", 1, JSON.stringify(DEFAULT_SETTINGS), iso));
  const today = todayInZone("UTC", now);
  const firstFriday = today <= PACKAGE_DATE ? nextFriday(PACKAGE_DATE) : nextFriday(today, true);
  const event = createDraftEvent({ series_id: SERIES_ID, date: firstFriday, timezone: null });
  statements.push(env.DB.prepare("INSERT OR IGNORE INTO events (id, date, status, revision, doc, updated_at) VALUES (?, ?, ?, ?, ?, ?)").bind(event.id, event.date, event.status, event.revision, JSON.stringify(event), iso));
  await env.DB.batch(statements);
}
__name(ensureSeeded, "ensureSeeded");
async function loadMembers(env) {
  const rows = await env.DB.prepare("SELECT doc FROM members ORDER BY id").all();
  return rows.results.map((r) => JSON.parse(r.doc));
}
__name(loadMembers, "loadMembers");
async function loadMember(env, id) {
  const row = await env.DB.prepare("SELECT doc FROM members WHERE id = ?").bind(id).first();
  if (!row) throw new HttpError(404, "not_found", "Member not found.");
  return JSON.parse(row.doc);
}
__name(loadMember, "loadMember");
async function saveMember(env, member, now) {
  await env.DB.prepare("UPDATE members SET doc = ?, updated_at = ? WHERE id = ?").bind(JSON.stringify(member), now.toISOString(), member.id).run();
}
__name(saveMember, "saveMember");
async function loadEvents(env) {
  const rows = await env.DB.prepare("SELECT doc FROM events ORDER BY date").all();
  return rows.results.map((r) => JSON.parse(r.doc));
}
__name(loadEvents, "loadEvents");
async function loadEvent(env, id) {
  const row = await env.DB.prepare("SELECT doc FROM events WHERE id = ?").bind(id).first();
  if (!row) throw new HttpError(404, "not_found", "Event not found.");
  return JSON.parse(row.doc);
}
__name(loadEvent, "loadEvent");
function auditStatement(env, a) {
  return env.DB.prepare("INSERT INTO audit (id, event_id, actor_id, action, before, after, reason, timestamp) VALUES (?, ?, ?, ?, ?, ?, ?, ?)").bind(
    a.id,
    a.event_id,
    a.actor_id,
    a.action,
    JSON.stringify(a.before ?? null),
    JSON.stringify(a.after ?? null),
    a.reason ?? null,
    a.timestamp
  );
}
__name(auditStatement, "auditStatement");
async function saveEventCas(env, previous, next, audit3, now) {
  const update = env.DB.prepare("UPDATE events SET doc = ?, revision = ?, status = ?, date = ?, updated_at = ? WHERE id = ? AND revision = ?").bind(
    JSON.stringify(next),
    next.revision,
    next.status,
    next.date,
    now.toISOString(),
    next.id,
    previous.revision
  );
  const results = await env.DB.batch([update, ...audit3.map((a) => auditStatement(env, a))]);
  const changes = results[0]?.meta?.changes ?? 0;
  if (changes !== 1) {
    throw new HttpError(409, "stale_revision", "Someone else changed this event. Reload to compare.");
  }
}
__name(saveEventCas, "saveEventCas");
async function insertEvent(env, event, now) {
  const res = await env.DB.prepare("INSERT OR IGNORE INTO events (id, date, status, revision, doc, updated_at) VALUES (?, ?, ?, ?, ?, ?)").bind(event.id, event.date, event.status, event.revision, JSON.stringify(event), now.toISOString()).run();
  return (res.meta?.changes ?? 0) === 1;
}
__name(insertEvent, "insertEvent");
async function loadDocument(env, key) {
  const row = await env.DB.prepare("SELECT doc, revision FROM documents WHERE key = ?").bind(key).first();
  if (!row) throw new HttpError(404, "not_found", `${key} not found.`);
  return { doc: JSON.parse(row.doc), revision: row.revision };
}
__name(loadDocument, "loadDocument");
async function saveDocumentCas(env, key, previousRevision, doc, revision, audit3, now) {
  const update = env.DB.prepare("UPDATE documents SET doc = ?, revision = ?, updated_at = ? WHERE key = ? AND revision = ?").bind(JSON.stringify(doc), revision, now.toISOString(), key, previousRevision);
  const results = await env.DB.batch([update, ...audit3.map((a) => auditStatement(env, a))]);
  if ((results[0]?.meta?.changes ?? 0) !== 1) throw new HttpError(409, "stale_revision", "Another leader changed this. Reload to compare.");
}
__name(saveDocumentCas, "saveDocumentCas");
async function loadOrganization(env) {
  const { doc } = await loadDocument(env, "organization");
  return doc;
}
__name(loadOrganization, "loadOrganization");
async function loadSettings(env) {
  const { doc } = await loadDocument(env, "settings");
  return { ...DEFAULT_SETTINGS, ...doc };
}
__name(loadSettings, "loadSettings");
async function recentAudit(env, limit = 200) {
  const rows = await env.DB.prepare("SELECT * FROM audit ORDER BY timestamp DESC LIMIT ?").bind(limit).all();
  return rows.results.map((r) => ({ id: r.id, event_id: r.event_id, actor_id: r.actor_id, action: r.action, before: JSON.parse(r.before), after: JSON.parse(r.after), reason: r.reason ?? void 0, timestamp: r.timestamp })).reverse();
}
__name(recentAudit, "recentAudit");

// src/router.ts
var Router = class {
  static {
    __name(this, "Router");
  }
  routes = [];
  add(method, path, handler) {
    const keys = [];
    const pattern = new RegExp(
      "^" + path.replace(/\/:([A-Za-z_]+)/g, (_m, key) => {
        keys.push(key);
        return "/([^/]+)";
      }) + "/?$"
    );
    this.routes.push({ method, pattern, keys, handler });
    return this;
  }
  get(path, handler) {
    return this.add("GET", path, handler);
  }
  post(path, handler) {
    return this.add("POST", path, handler);
  }
  delete(path, handler) {
    return this.add("DELETE", path, handler);
  }
  match(method, pathname) {
    for (const r of this.routes) {
      if (r.method !== method) continue;
      const m = r.pattern.exec(pathname);
      if (!m) continue;
      const params = {};
      r.keys.forEach((k, i) => params[k] = decodeURIComponent(m[i + 1]));
      return { handler: r.handler, params };
    }
    return null;
  }
};
async function readJson(request) {
  const text = await request.text();
  if (!text) return {};
  try {
    const parsed = JSON.parse(text);
    if (parsed && typeof parsed === "object" && !Array.isArray(parsed)) return parsed;
    throw new Error("not an object");
  } catch {
    throw new HttpError(400, "bad_json", "Request body must be a JSON object.");
  }
}
__name(readJson, "readJson");
function str(body, key, required = true) {
  const v = body[key];
  if (typeof v === "string" && v.length > 0) return v;
  if (!required && (v === void 0 || v === null || v === "")) return "";
  throw new HttpError(400, "bad_request", `Missing or invalid "${key}".`);
}
__name(str, "str");
function num(body, key, required = true) {
  const v = body[key];
  if (typeof v === "number" && Number.isFinite(v)) return v;
  if (!required && (v === void 0 || v === null)) return void 0;
  throw new HttpError(400, "bad_request", `Missing or invalid "${key}".`);
}
__name(num, "num");
function corsHeaders(env, request) {
  const origin = request.headers.get("origin") ?? "";
  const allowed = (env.ALLOWED_ORIGINS ?? "").split(",").map((s) => s.trim()).filter(Boolean);
  const headers = {
    "access-control-allow-methods": "GET,POST,DELETE,OPTIONS",
    "access-control-allow-headers": "authorization,content-type",
    "access-control-max-age": "86400",
    vary: "origin"
  };
  if (origin && (allowed.includes(origin) || allowed.includes("*"))) headers["access-control-allow-origin"] = origin;
  return headers;
}
__name(corsHeaders, "corsHeaders");

// src/index.ts
var router = new Router();
function actorFor(account) {
  return account.member_id ?? `account:${account.id}`;
}
__name(actorFor, "actorFor");
function ctxFor(account, now) {
  return { actor: actorFor(account), now: now.toISOString() };
}
__name(ctxFor, "ctxFor");
function stripPrivate(members, account) {
  if (account?.role === "leader") return members;
  return members.map((m) => {
    const { mechanical_notes: _omit, ...rest } = m;
    void _omit;
    return rest;
  });
}
__name(stripPrivate, "stripPrivate");
function mapError(err) {
  if (err instanceof HttpError) throw err;
  if (err instanceof LifecycleError) {
    const status = err.code === "stale_revision" ? 409 : err.code === "forbidden" ? 403 : 422;
    throw new HttpError(status, err.code, err.message);
  }
  throw err;
}
__name(mapError, "mapError");
async function mutateEvent(ctx, id, fn) {
  const event = await loadEvent(ctx.env, id);
  let res;
  try {
    res = fn(event);
  } catch (err) {
    mapError(err);
  }
  await saveEventCas(ctx.env, event, res.event, res.audit, ctx.now);
  return res.event;
}
__name(mutateEvent, "mutateEvent");
async function mutateOrganization(ctx, fn) {
  const { doc, revision } = await loadDocument(ctx.env, "organization");
  let res;
  try {
    res = fn(doc);
  } catch (err) {
    mapError(err);
  }
  await saveDocumentCas(ctx.env, "organization", revision, res.state, res.state.revision, res.audit, ctx.now);
  return { organization: res.state, inverse: res.inverse };
}
__name(mutateOrganization, "mutateOrganization");
function expected(body) {
  return num(body, "expected_revision", false);
}
__name(expected, "expected");
router.get("/health", async () => ({ ok: true, service: "stry-alliance-api" }));
router.post("/auth/register", async (ctx) => {
  const body = await ctx.body();
  const username = validateUsername(str(body, "username"));
  const password = str(body, "password");
  validatePassword(password);
  const inviteCode = str(body, "invite_code");
  const memberId = str(body, "member_id", false) || null;
  if (await findAccountByUsername(ctx.env, username)) throw new HttpError(409, "username_taken", "That username is already taken.");
  if (memberId) await loadMember(ctx.env, memberId);
  let role = "member";
  let verified = false;
  const invite = await ctx.env.DB.prepare("SELECT * FROM invites WHERE code = ?").bind(inviteCode).first();
  if (invite && invite.uses_left > 0 && invite.expires_at > ctx.now.toISOString()) {
    role = invite.role;
    await ctx.env.DB.prepare("UPDATE invites SET uses_left = uses_left - 1 WHERE code = ? AND uses_left > 0").bind(invite.code).run();
  } else if (ctx.env.OWNER_SETUP_CODE && inviteCode === ctx.env.OWNER_SETUP_CODE && await leaderCount(ctx.env) === 0) {
    role = "leader";
    verified = true;
  } else {
    throw new HttpError(403, "bad_invite", "That invite code is not valid. Ask a leader for a new one.");
  }
  const { hash, salt } = await hashPassword(password);
  const id = newId("acct");
  await ctx.env.DB.prepare("INSERT INTO accounts (id, username, password_hash, salt, role, member_id, verified, disabled, created_at) VALUES (?, ?, ?, ?, ?, ?, ?, 0, ?)").bind(id, username, hash, salt, role, memberId, verified ? 1 : 0, ctx.now.toISOString()).run();
  const token = await createSession(ctx.env, id, ctx.now);
  const account = { id, username, role, member_id: memberId, verified, disabled: false, created_at: ctx.now.toISOString() };
  return { token, account };
});
router.post("/auth/login", async (ctx) => {
  const body = await ctx.body();
  const username = str(body, "username").trim();
  const password = str(body, "password");
  const row = await findAccountByUsername(ctx.env, username);
  if (!row || row.disabled === 1 || !await verifyPassword(password, row.password_hash, row.salt)) {
    throw new HttpError(401, "bad_credentials", "Wrong username or password.");
  }
  const token = await createSession(ctx.env, row.id, ctx.now);
  return { token, account: rowToAccount(row) };
});
router.post("/auth/logout", async (ctx) => {
  const header = ctx.request.headers.get("authorization") ?? "";
  const token = header.replace(/^Bearer\s+/i, "").trim();
  if (token) await deleteSession(ctx.env, token);
  return { ok: true };
});
router.post("/auth/password", async (ctx) => {
  const account = requireAccount(ctx.account);
  const body = await ctx.body();
  const current = str(body, "current_password");
  const next = str(body, "new_password");
  validatePassword(next);
  const row = await findAccountByUsername(ctx.env, account.username);
  if (!row || !await verifyPassword(current, row.password_hash, row.salt)) throw new HttpError(401, "bad_credentials", "Current password is wrong.");
  const { hash, salt } = await hashPassword(next);
  await ctx.env.DB.prepare("UPDATE accounts SET password_hash = ?, salt = ? WHERE id = ?").bind(hash, salt, account.id).run();
  return { ok: true };
});
router.get("/me", async (ctx) => ({ account: requireAccount(ctx.account) }));
router.get("/roster", async (ctx) => {
  const members = await loadMembers(ctx.env);
  return { roster: members.filter((m) => m.active).map((m) => ({ id: m.id, username: m.username })) };
});
router.get("/state", async (ctx) => {
  const account = requireAccount(ctx.account);
  const [members, events, organization, settings, audit3] = await Promise.all([
    loadMembers(ctx.env),
    loadEvents(ctx.env),
    loadOrganization(ctx.env),
    loadSettings(ctx.env),
    account.role === "leader" ? recentAudit(ctx.env) : Promise.resolve([])
  ]);
  return { members: stripPrivate(members, account), events, organization, settings, audit: audit3, account, server_time: ctx.now.toISOString() };
});
router.get("/export", async (ctx) => {
  requireLeader(ctx.account);
  const [members, events, organization, settings, audit3] = await Promise.all([loadMembers(ctx.env), loadEvents(ctx.env), loadOrganization(ctx.env), loadSettings(ctx.env), recentAudit(ctx.env, 5e3)]);
  return { exported_at: ctx.now.toISOString(), members, events, organization, settings, audit: audit3 };
});
router.post("/events/:id/availability", async (ctx) => {
  const account = requireAccount(ctx.account);
  const body = await ctx.body();
  const choice = str(body, "choice");
  if (!["team1", "team2", "either", "unavailable"].includes(choice)) throw new HttpError(400, "bad_request", "Invalid availability choice.");
  const requested = str(body, "member_id", false) || account.member_id;
  if (!requested) throw new HttpError(400, "no_member", "Link your roster member first.");
  const self = requested === account.member_id;
  if (!self && account.role !== "leader") throw new HttpError(403, "forbidden", "You can only change your own availability.");
  await loadMember(ctx.env, requested);
  const event = await mutateEvent(ctx, ctx.params.id, (e) => setAvailability(e, requested, choice, ctxFor(account, ctx.now), self ? "self" : actorFor(account)));
  return { event };
});
router.post("/events/:id/availability/fill", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const choice = str(body, "choice");
  if (!["team1", "team2", "either", "unavailable"].includes(choice)) throw new HttpError(400, "bad_request", "Invalid availability choice.");
  const members = await loadMembers(ctx.env);
  let count = 0;
  const event = await mutateEvent(ctx, ctx.params.id, (e) => {
    let next = e;
    const audit3 = [];
    for (const m of members) {
      if (!m.active || next.availability[m.id]) continue;
      const r = setAvailability(next, m.id, choice, ctxFor(account, ctx.now), actorFor(account));
      next = r.event;
      audit3.push(...r.audit);
      count++;
    }
    return { event: next, audit: audit3 };
  });
  return { event, count };
});
router.post("/events/:id/generate", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const [members, events] = await Promise.all([loadMembers(ctx.env), loadEvents(ctx.env)]);
  const event = await mutateEvent(ctx, ctx.params.id, (e) => applySuggestions(e, members, events, ctxFor(account, ctx.now), expected(body) ?? e.revision));
  return { event };
});
router.post("/events/:id/lock", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const event = await mutateEvent(ctx, ctx.params.id, (e) => lockMember(e, str(body, "member_id"), str(body, "team_id"), str(body, "reason"), ctxFor(account, ctx.now), expected(body)));
  return { event };
});
router.post("/events/:id/unlock", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const event = await mutateEvent(ctx, ctx.params.id, (e) => unlockMember(e, str(body, "member_id"), ctxFor(account, ctx.now), expected(body)));
  return { event };
});
router.post("/events/:id/move", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const target = str(body, "target");
  const event = await mutateEvent(ctx, ctx.params.id, (e) => moveMember(e, str(body, "member_id"), target === "reserve" ? "reserve" : target, ctxFor(account, ctx.now), expected(body)));
  return { event };
});
router.post("/events/:id/swap", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const event = await mutateEvent(ctx, ctx.params.id, (e) => swapMembers(e, str(body, "a"), str(body, "b"), ctxFor(account, ctx.now), expected(body)));
  return { event };
});
router.post("/events/:id/restore", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const assignments = body.assignments;
  if (!Array.isArray(assignments)) throw new HttpError(400, "bad_request", 'Missing "assignments".');
  const event = await mutateEvent(ctx, ctx.params.id, (e) => replaceAssignments(e, assignments, ctxFor(account, ctx.now), expected(body)));
  return { event };
});
router.post("/events/:id/publish", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const event = await mutateEvent(ctx, ctx.params.id, (e) => publishEvent(e, ctxFor(account, ctx.now), expected(body)));
  return { event };
});
router.post("/events/:id/confirm", async (ctx) => {
  const account = requireAccount(ctx.account);
  const body = await ctx.body();
  const requested = str(body, "member_id", false) || account.member_id;
  if (!requested) throw new HttpError(400, "no_member", "Link your roster member first.");
  if (requested !== account.member_id && account.role !== "leader") throw new HttpError(403, "forbidden", "You can only confirm your own assignment.");
  const event = await mutateEvent(ctx, ctx.params.id, (e) => confirmAssignment(e, requested, ctxFor(account, ctx.now)));
  return { event };
});
router.post("/events/:id/attendance", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const outcome = str(body, "outcome");
  if (!["played", "no_show", "withdrew", "unused_reserve", "unknown"].includes(outcome)) throw new HttpError(400, "bad_request", "Invalid outcome.");
  const memberId = str(body, "member_id");
  await loadMember(ctx.env, memberId);
  const opts = {};
  if ("team_id" in body) opts.team_id = body.team_id ?? null;
  if (typeof body.substitute === "boolean") opts.substitute = body.substitute;
  const event = await mutateEvent(ctx, ctx.params.id, (e) => recordAttendance(e, memberId, outcome, ctxFor(account, ctx.now), opts));
  return { event };
});
router.post("/events/:id/finalize", async (ctx) => {
  const account = requireLeader(ctx.account);
  const settings = await loadSettings(ctx.env);
  const event = await mutateEvent(ctx, ctx.params.id, (e) => finalizeEvent(e, ctxFor(account, ctx.now)));
  const events = await loadEvents(ctx.env);
  const next = ensureNextWeekDraft(events, { series_id: SERIES_ID, afterDate: event.date, timezone: event.timezone ?? settings.timezone, team_times: settings.default_team_times });
  if (next.created) await insertEvent(ctx.env, next.event, ctx.now);
  return { event, next_event: next.event };
});
router.post("/events/:id/cancel", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const event = await mutateEvent(ctx, ctx.params.id, (e) => cancelEvent(e, ctxFor(account, ctx.now), str(body, "reason", false) || "Canceled by leader"));
  return { event };
});
router.post("/events/:id/schedule", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const patch = {};
  if (typeof body.date === "string") patch.date = body.date;
  if ("timezone" in body) patch.timezone = typeof body.timezone === "string" && body.timezone ? body.timezone : null;
  if (typeof body.date_confirmed === "boolean") patch.date_confirmed = body.date_confirmed;
  if (body.team_times && typeof body.team_times === "object") patch.team_times = body.team_times;
  const event = await mutateEvent(ctx, ctx.params.id, (e) => updateSchedule(e, patch, ctxFor(account, ctx.now)));
  return { event };
});
router.post("/events/next-week", async (ctx) => {
  requireLeader(ctx.account);
  const [events, settings] = await Promise.all([loadEvents(ctx.env), loadSettings(ctx.env)]);
  const latest = [...events].filter((e) => e.status !== "canceled").sort((a, b) => a.date < b.date ? 1 : -1)[0];
  const afterDate = latest ? latest.date : todayInZone(settings.timezone ?? "UTC", ctx.now);
  const next = ensureNextWeekDraft(events, { series_id: SERIES_ID, afterDate, timezone: settings.timezone, team_times: settings.default_team_times });
  if (next.created) await insertEvent(ctx.env, next.event, ctx.now);
  return { event: next.event, created: next.created };
});
router.post("/organization/slots", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const edits = body.edits;
  if (!Array.isArray(edits) || edits.length === 0) throw new HttpError(400, "bad_request", 'Missing "edits".');
  const result = await mutateOrganization(ctx, (org) => applySlotEdits(org, edits, ctxFor(account, ctx.now), expected(body) ?? org.revision));
  return result;
});
router.post("/organization/tasks", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const op = str(body, "op");
  const result = await mutateOrganization(ctx, (org) => {
    const rev = expected(body) ?? org.revision;
    const c = ctxFor(account, ctx.now);
    switch (op) {
      case "add":
        return addTask(org, str(body, "title"), c, rev);
      case "rename":
        return renameTask(org, str(body, "id"), str(body, "title"), c, rev);
      case "archive":
        return setTaskArchived(org, str(body, "id"), body.archived !== false, c, rev);
      case "reorder":
        return reorderTask(org, str(body, "id"), (num(body, "direction") ?? 1) < 0 ? -1 : 1, c, rev);
      default:
        throw new HttpError(400, "bad_request", "Unknown task op.");
    }
  });
  return result;
});
router.post("/organization/mapping", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const memberId = str(body, "member_id", false) || null;
  if (memberId) await loadMember(ctx.env, memberId);
  const result = await mutateOrganization(ctx, (org) => setNameMapping(org, str(body, "source_name"), memberId, ctxFor(account, ctx.now), expected(body) ?? org.revision));
  return result;
});
router.post("/members/:id", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const member = await loadMember(ctx.env, ctx.params.id);
  let next = member;
  if (typeof body.mechanical_notes === "string") next = withMechanicalNote(next, body.mechanical_notes);
  if (typeof body.active === "boolean") next = { ...next, active: body.active };
  await saveMember(ctx.env, next, ctx.now);
  await ctx.env.DB.batch([
    auditStatement(ctx.env, { id: `${ctx.now.toISOString()}-member-${randomToken(4)}`, event_id: null, actor_id: actorFor(account), action: "member.update", before: { active: member.active, has_notes: !!member.mechanical_notes }, after: { active: next.active, has_notes: !!next.mechanical_notes }, timestamp: ctx.now.toISOString() })
  ]);
  return { member: next };
});
router.post("/settings", async (ctx) => {
  const account = requireLeader(ctx.account);
  const body = await ctx.body();
  const { doc, revision } = await loadDocument(ctx.env, "settings");
  const next = { ...doc };
  if ("timezone" in body) {
    const tz = typeof body.timezone === "string" && body.timezone ? body.timezone : null;
    if (tz && !isValidTimeZone(tz)) throw new HttpError(400, "bad_timezone", "Unknown timezone name.");
    next.timezone = tz;
  }
  if (body.default_team_times && typeof body.default_team_times === "object") {
    const t = body.default_team_times;
    next.default_team_times = { team1: t.team1 ?? doc.default_team_times.team1, team2: t.team2 ?? doc.default_team_times.team2 };
  }
  await saveDocumentCas(ctx.env, "settings", revision, next, revision + 1, [{ id: `${ctx.now.toISOString()}-settings-${randomToken(4)}`, event_id: null, actor_id: actorFor(account), action: "settings.update", before: doc, after: next, timestamp: ctx.now.toISOString() }], ctx.now);
  return { settings: next };
});
router.get("/accounts", async (ctx) => {
  requireLeader(ctx.account);
  const rows = await ctx.env.DB.prepare("SELECT id, username, role, member_id, verified, disabled, created_at FROM accounts ORDER BY created_at").all();
  return { accounts: rows.results.map((r) => ({ id: r.id, username: r.username, role: r.role, member_id: r.member_id, verified: r.verified === 1, disabled: r.disabled === 1, created_at: r.created_at })) };
});
router.post("/accounts/:id", async (ctx) => {
  const leader = requireLeader(ctx.account);
  const body = await ctx.body();
  const row = await ctx.env.DB.prepare("SELECT id, role, disabled FROM accounts WHERE id = ?").bind(ctx.params.id).first();
  if (!row) throw new HttpError(404, "not_found", "Account not found.");
  const sets = [];
  const values = [];
  if (body.role === "leader" || body.role === "member") {
    if (row.id === leader.id && body.role === "member" && await leaderCount(ctx.env) <= 1) throw new HttpError(422, "last_leader", "You are the only leader; promote someone else first.");
    sets.push("role = ?");
    values.push(body.role);
  }
  if ("member_id" in body) {
    const memberId = typeof body.member_id === "string" && body.member_id ? body.member_id : null;
    if (memberId) await loadMember(ctx.env, memberId);
    sets.push("member_id = ?");
    values.push(memberId);
  }
  if (typeof body.verified === "boolean") {
    sets.push("verified = ?");
    values.push(body.verified ? 1 : 0);
  }
  if (typeof body.disabled === "boolean") {
    if (row.id === leader.id && body.disabled) throw new HttpError(422, "self_disable", "You cannot disable your own account.");
    sets.push("disabled = ?");
    values.push(body.disabled ? 1 : 0);
  }
  if (!sets.length) throw new HttpError(400, "bad_request", "Nothing to update.");
  await ctx.env.DB.prepare(`UPDATE accounts SET ${sets.join(", ")} WHERE id = ?`).bind(...values, row.id).run();
  if (body.disabled === true) await ctx.env.DB.prepare("DELETE FROM sessions WHERE account_id = ?").bind(row.id).run();
  const updated = await ctx.env.DB.prepare("SELECT id, username, role, member_id, verified, disabled, created_at FROM accounts WHERE id = ?").bind(row.id).first();
  await ctx.env.DB.batch([auditStatement(ctx.env, { id: `${ctx.now.toISOString()}-acct-${randomToken(4)}`, event_id: null, actor_id: actorFor(leader), action: "account.update", before: { id: row.id }, after: body, timestamp: ctx.now.toISOString() })]);
  return { account: updated && { ...updated, verified: updated.verified === 1, disabled: updated.disabled === 1 } };
});
router.get("/invites", async (ctx) => {
  requireLeader(ctx.account);
  const rows = await ctx.env.DB.prepare("SELECT code, role, created_by, uses_left, expires_at, created_at FROM invites WHERE uses_left > 0 AND expires_at > ? ORDER BY created_at DESC").bind(ctx.now.toISOString()).all();
  return { invites: rows.results };
});
router.post("/invites", async (ctx) => {
  const leader = requireLeader(ctx.account);
  const body = await ctx.body();
  const role = body.role === "leader" ? "leader" : "member";
  const uses = Math.min(500, Math.max(1, Math.floor(num(body, "uses", false) ?? 25)));
  const days = Math.min(365, Math.max(1, Math.floor(num(body, "days", false) ?? 14)));
  const code = randomToken(6).replace(/[-_]/g, "x").slice(0, 8).toUpperCase();
  const expires = new Date(ctx.now.getTime() + days * 864e5).toISOString();
  await ctx.env.DB.prepare("INSERT INTO invites (code, role, created_by, uses_left, expires_at, created_at) VALUES (?, ?, ?, ?, ?, ?)").bind(code, role, leader.id, uses, expires, ctx.now.toISOString()).run();
  return { invite: { code, role, uses_left: uses, expires_at: expires } };
});
router.delete("/invites/:code", async (ctx) => {
  requireLeader(ctx.account);
  await ctx.env.DB.prepare("DELETE FROM invites WHERE code = ?").bind(ctx.params.code).run();
  return { ok: true };
});
var src_default = {
  async fetch(request, env) {
    const cors = corsHeaders(env, request);
    if (request.method === "OPTIONS") return new Response(null, { status: 204, headers: cors });
    const url = new URL(request.url);
    const now = /* @__PURE__ */ new Date();
    const json = /* @__PURE__ */ __name((status, data) => new Response(JSON.stringify(data), { status, headers: { "content-type": "application/json; charset=utf-8", "cache-control": "no-store", ...cors } }), "json");
    try {
      const match = router.match(request.method, url.pathname);
      if (!match) return json(404, { error: "not_found", message: `No route for ${request.method} ${url.pathname}` });
      await ensureSeeded(env, now);
      const account = await accountFromRequest(env, request, now);
      let cached = null;
      const ctx = { env, request, account, params: match.params, now, body: /* @__PURE__ */ __name(() => cached ??= readJson(request), "body") };
      const data = await match.handler(ctx);
      return json(200, data);
    } catch (err) {
      if (err instanceof HttpError) return json(err.status, { error: err.code, message: err.message });
      if (err instanceof LifecycleError) return json(err.code === "stale_revision" ? 409 : 422, { error: err.code, message: err.message });
      console.error(err);
      return json(500, { error: "internal", message: "Something went wrong on the server." });
    }
  }
};

// ../node_modules/wrangler/templates/middleware/middleware-ensure-req-body-drained.ts
var drainBody = /* @__PURE__ */ __name(async (request, env, _ctx, middlewareCtx) => {
  try {
    return await middlewareCtx.next(request, env);
  } finally {
    try {
      if (request.body !== null && !request.bodyUsed) {
        const reader = request.body.getReader();
        while (!(await reader.read()).done) {
        }
      }
    } catch (e) {
      console.error("Failed to drain the unused request body.", e);
    }
  }
}, "drainBody");
var middleware_ensure_req_body_drained_default = drainBody;

// ../node_modules/wrangler/templates/middleware/middleware-miniflare3-json-error.ts
function reduceError(e) {
  return {
    name: e?.name,
    message: e?.message ?? String(e),
    stack: e?.stack,
    cause: e?.cause === void 0 ? void 0 : reduceError(e.cause)
  };
}
__name(reduceError, "reduceError");
var jsonError = /* @__PURE__ */ __name(async (request, env, _ctx, middlewareCtx) => {
  try {
    return await middlewareCtx.next(request, env);
  } catch (e) {
    const error = reduceError(e);
    const body = JSON.stringify(error);
    const headers = {
      "Content-Type": "application/json",
      "MF-Experimental-Error-Stack": "true"
    };
    const encoded = encodeURIComponent(body);
    if (encoded.length <= 8192) {
      headers["MF-Experimental-Error-Stack-Payload"] = encoded;
    }
    return new Response(body, { status: 500, headers });
  }
}, "jsonError");
var middleware_miniflare3_json_error_default = jsonError;

// .wrangler/tmp/bundle-UMmW03/middleware-insertion-facade.js
var __INTERNAL_WRANGLER_MIDDLEWARE__ = [
  middleware_ensure_req_body_drained_default,
  middleware_miniflare3_json_error_default
];
var middleware_insertion_facade_default = src_default;

// ../node_modules/wrangler/templates/middleware/common.ts
var __facade_middleware__ = [];
function __facade_register__(...args) {
  __facade_middleware__.push(...args.flat());
}
__name(__facade_register__, "__facade_register__");
function __facade_invokeChain__(request, env, ctx, dispatch, middlewareChain) {
  const [head, ...tail] = middlewareChain;
  const middlewareCtx = {
    dispatch,
    next(newRequest, newEnv) {
      return __facade_invokeChain__(newRequest, newEnv, ctx, dispatch, tail);
    }
  };
  return head(request, env, ctx, middlewareCtx);
}
__name(__facade_invokeChain__, "__facade_invokeChain__");
function __facade_invoke__(request, env, ctx, dispatch, finalMiddleware) {
  return __facade_invokeChain__(request, env, ctx, dispatch, [
    ...__facade_middleware__,
    finalMiddleware
  ]);
}
__name(__facade_invoke__, "__facade_invoke__");

// .wrangler/tmp/bundle-UMmW03/middleware-loader.entry.ts
var __Facade_ScheduledController__ = class ___Facade_ScheduledController__ {
  constructor(scheduledTime, cron, noRetry) {
    this.scheduledTime = scheduledTime;
    this.cron = cron;
    this.#noRetry = noRetry;
  }
  scheduledTime;
  cron;
  static {
    __name(this, "__Facade_ScheduledController__");
  }
  #noRetry;
  noRetry() {
    if (!(this instanceof ___Facade_ScheduledController__)) {
      throw new TypeError("Illegal invocation");
    }
    this.#noRetry();
  }
};
function wrapExportedHandler(worker) {
  if (__INTERNAL_WRANGLER_MIDDLEWARE__ === void 0 || __INTERNAL_WRANGLER_MIDDLEWARE__.length === 0) {
    return worker;
  }
  for (const middleware of __INTERNAL_WRANGLER_MIDDLEWARE__) {
    __facade_register__(middleware);
  }
  const fetchDispatcher = /* @__PURE__ */ __name(function(request, env, ctx) {
    if (worker.fetch === void 0) {
      throw new Error("Handler does not export a fetch() function.");
    }
    return worker.fetch(request, env, ctx);
  }, "fetchDispatcher");
  return {
    ...worker,
    fetch(request, env, ctx) {
      const dispatcher = /* @__PURE__ */ __name(function(type, init) {
        if (type === "scheduled" && worker.scheduled !== void 0) {
          const controller = new __Facade_ScheduledController__(
            Date.now(),
            init.cron ?? "",
            () => {
            }
          );
          return worker.scheduled(controller, env, ctx);
        }
      }, "dispatcher");
      return __facade_invoke__(request, env, ctx, dispatcher, fetchDispatcher);
    }
  };
}
__name(wrapExportedHandler, "wrapExportedHandler");
function wrapWorkerEntrypoint(klass) {
  if (__INTERNAL_WRANGLER_MIDDLEWARE__ === void 0 || __INTERNAL_WRANGLER_MIDDLEWARE__.length === 0) {
    return klass;
  }
  for (const middleware of __INTERNAL_WRANGLER_MIDDLEWARE__) {
    __facade_register__(middleware);
  }
  return class extends klass {
    #fetchDispatcher = /* @__PURE__ */ __name((request, env, ctx) => {
      this.env = env;
      this.ctx = ctx;
      if (super.fetch === void 0) {
        throw new Error("Entrypoint class does not define a fetch() function.");
      }
      return super.fetch(request);
    }, "#fetchDispatcher");
    #dispatcher = /* @__PURE__ */ __name((type, init) => {
      if (type === "scheduled" && super.scheduled !== void 0) {
        const controller = new __Facade_ScheduledController__(
          Date.now(),
          init.cron ?? "",
          () => {
          }
        );
        return super.scheduled(controller);
      }
    }, "#dispatcher");
    fetch(request) {
      return __facade_invoke__(
        request,
        this.env,
        this.ctx,
        this.#dispatcher,
        this.#fetchDispatcher
      );
    }
  };
}
__name(wrapWorkerEntrypoint, "wrapWorkerEntrypoint");
var WRAPPED_ENTRY;
if (typeof middleware_insertion_facade_default === "object") {
  WRAPPED_ENTRY = wrapExportedHandler(middleware_insertion_facade_default);
} else if (typeof middleware_insertion_facade_default === "function") {
  WRAPPED_ENTRY = wrapWorkerEntrypoint(middleware_insertion_facade_default);
}
var middleware_loader_entry_default = WRAPPED_ENTRY;
export {
  __INTERNAL_WRANGLER_MIDDLEWARE__,
  middleware_loader_entry_default as default
};
//# sourceMappingURL=index.js.map
